NOOR Codex of Light, the change archive
One line for every edit, addition and removal that reaches the site, newest first.
Form: date (UTC) | area | edited, added or removed | the files | what was done and why
Kept as plain text at noorcodex.com/changes.txt and linked from the console's System room.

2026-09-25 | social | added | api/_experiments.js, api/experiments.js, api/_schedule.js, api/social.js, api/_insights.js, api/observatory.js, api/lantern-agent.js, api/_agent.js, admin2.html, vercel.json, tests/experiments.mjs, tests/insights.mjs, tests/observatory.mjs, tests/agent.mjs, OPERATIONS.md, ARCHITECTURE.md, CURRENT_STATE.md, SOCIAL_ENGINE.md | Masterplan step 9, experiments and the learning loop. Every Instagram reel row now carries its length, its reciter for a verse, its average watch time and the share of the reel people watched, and the Observatory shows what holds attention by kind, by verse length and by reciter. One test can run at a time: the two versions alternate by day inside the normal line-up (no extra posts), the verdict waits for the full window and needs at least ten measured reels a side, p below 0.05 on watched share and a gap of ten points, and stopping a test stops it at once. The console's test card plans and stops a test; the Lantern can read it and propose one. The hourly post never waits on any of this: any fault falls back to the usual pick.
2026-09-25 | quran | edited | quran.html, tests/mushaf-reading.mjs, tests/quran-player.mjs, tests/mushaf.mjs, tests/mushaf-engine.mjs, tests/skin.mjs | The Qur'an room now matches the prototype approved on 17 September. Day is the default and Night is a choice that is remembered. The settings sheet opens in full at every window size (a shared bottom bar script was pinning it below the screen). The top bar is one quiet surah button with study and settings icons, the study companion opens as a sheet, the Arabic sits right aligned with its verse seal, each verse has one row of Listen, Study and Copy, the sounding verse carries a gold wash with the word being recited lit, and the player stays on the bottom edge from the moment a surah opens, light by day and dark by night.
2026-09-25 | lantern | edited | api/_agent.js, tests/agent.mjs | The first live run with all keys answered "no breakdown by hour" to a question about the hour, and offered a line-up proposal from the planner's own guess ("video posts at 3-4 PM") that the checked answer contradicted. A question about the hour now reads insights and numbers and keeps the hours first when the data is trimmed, and a proposal-only step waits for the checked answer and carries it.
2026-09-25 | lantern | edited | api/_llm.js, tests/llm.mjs | Gemini's newest flash answered 503 (busy) all morning while the key itself was fine, so the strong and long tiers now carry the next live flash name too before the walk leaves Gemini for Groq.
2026-09-25 | lantern | edited | api/_llm.js, api/lantern-models.js, tests/llm.mjs | The probe of every name found Groq's gpt-oss spending its whole budget thinking and returning nothing, and a free OpenRouter name writing its thinking into the reply. Groq's gpt-oss is now asked for low effort, OpenRouter is asked to keep the thinking out, any think block left in a reply is cut, and the probe gives each name room to answer.
2026-09-25 | lantern | edited | api/_llm.js, api/lantern-models.js, tests/llm.mjs | The first probe with all three keys set found every tier still opening on an OpenRouter name remembered from before the Groq and Gemini keys, so neither was ever asked. A remembered name now only leads inside the tier's own first provider, and the probe can ask every name once (each:true) so each provider is known to answer.
2026-09-25 | site | edited | api/page.js, tests/rooms.mjs | Mu'tah the battle no longer reads as Mut'ah the fiqh term in related words: a word written with its apostrophe in another place is not the same word; bare spellings such as Quran and dua still match
2026-09-24 | console | edited | api/observatory.js | The Observatory's first live notes read "the 14:00 UTC (a afternoon reel)" and "reels lead engagement": the hour and its kind of post now read "the 14:00 UTC afternoon reel", and a kind the shelf could not name draws no sentence at all.
2026-09-24 | lantern | edited | api/_llm.js | The first live probe of the router, with only the OpenRouter key set, found one free name in every tier and that name refused the house (403, "only available on agentic harnesses"), so the Lantern could not plan or answer. A tier now walks up to four of OpenRouter's free names, a name that refuses with 403 or 404 is set aside for a day, and OpenRouter's daily allowance is counted once for the whole account rather than per name, since that is how OpenRouter counts it.
2026-09-24 | site | edited | api/_insights.js, api/observatory.js, api/warm.js, api/insights.js, api/_agent.js, api/lantern-agent.js, vercel.json, tests/insights.mjs, tests/observatory.mjs, tests/agent.mjs | The Lantern agent, part 6: a second production run asked "which kind of reel reaches most people on Instagram, and what should we post more of", and the answer said only generic reels were posted, "reel:reel", 106 of them, and recommended posting more of the very kind the house could not name. Four real defects, all fixed. KIND AT READ TIME: snapshot() (api/_insights.js) bakes a slot record's kind into the store forever the day it is written; the night warm.js's own manifest fetch answered nothing (a network call to the site's own /reels/index.json, from inside the site, that a slow cold start can simply fail with no error anyone sees), every reel that day was written under the catch-all "reel:reel" and stayed that way, since numbers() and observatory.js's own kind folds trusted the stored kind forever after and never asked the shelf again. A new reclassifyKind(rec, manifest, card) is asked of every stored kind that is still "reel:reel" or missing, with whatever manifest THIS read has, before numbers()'s byKind/best/worst and observatory.js's walkStats build their own kindDaily/kindTotals/bySubject folds; a kind already known is never second-guessed. The shelf itself is now read locally first: a new localManifest() (api/_insights.js) reads reels/index.json straight off disk the way observatory.js's own shelfNow() already did, and a new shelfManifest(opts) tries that first, an HTTP fetch of the caller's own site only as the very last resort; collect(), snapshot() and numbers() all default to localManifest() when a caller hands in no manifest at all (this is what gives api/house.js's own flow action, which never fetched a manifest in the first place, the shelf too, with no change to api/_flow.js needed); api/warm.js's own inline fetch and api/insights.js's own manifest() helper both now call shelfManifest() instead; vercel.json's includeFiles for api/insights.js, api/warm.js and api/house.js gained reels/index.json alongside assets/reel-subjects.json, both under the 256-character glob limit. KIND_LABEL's own "reel:reel" entry now reads "reels whose kind could not be matched" rather than the bare word "reels", so even a title the shelf truly cannot name is never shown as the raw internal id. PLAN COVERAGE (api/_agent.js): the planner parsed this time, a real model answered valid JSON, but named observatory alone for a question its own keyword map (fallbackPlan's, part 5) already knows needs insights and numbers. A new ensurePlanCoverage(steps, message), run after every validatePlan() regardless of whether the plan came from a model or the fallback, tops up a plan the model actually produced (never replacing it) with whatever keywordTools(q) (the same keyword map, factored out so fallbackPlan and this share one reading of the question) finds missing, and always adds the analyst and strategist subagents for a question asking "what should", "why" or naming "the plan", capped at the same step budget throughout. NO INTERNAL ID REACHES A MODEL: a tool's own JSON carries "reel:verse" or a slot id like "reelA" because that is how the house keeps the record, and the second production run read "reel:reel" straight out of it. api/lantern-agent.js gains humanizeIds(), a recursive walk that replaces any value or object key shaped like a kind id (kindLabel(), including the catch-all) or a bare slot id (observatory.js's own SLOT_NOUN, now exported as slotLabel()) with the same word the owner already reads elsewhere in the console; every tool in buildTools() is wrapped once, at the end, so this reaches every tool's own `data` including one added here next, with no per-tool patching to remember. EVERY FIGURE LABELS ITS OWN WINDOW: kindTotals and bySubject (30-day folds) sat beside summary (a 7-day fold) in the same JSON with only one top-level windowDays (30) to read, which a run misread as the count for "this week"; summary now carries its own windowDays: 7 beside the top-level windowDays: 30 (documented as the trend and kind history's own span), and every kindTotals and bySubject row carries its own windowDays: 30. tests/insights.mjs gains a block proving a stored "reel:reel" whose title matches a real shelf hook is reclassified under its true kind by numbers(), that reclassifyKind never second-guesses a kind already known, and that the local shelf read never falls through to an HTTP fetch while it is there; tests/observatory.mjs gains a matching block for walkStats' own kindTotals and kindDaily, plus a block proving summary's, kindTotals' and bySubject's own windowDays are stated and distinct; tests/agent.mjs gains a block proving ensurePlanCoverage tops up a real model's own under-reaching plan, never pads one that already covers the question, and never exceeds the step budget, and a block feeding humanizeIds a realistic nested fixture proving no /\breel:\w+/ or slot id survives anywhere in the walked JSON, that a key shaped like a kind id is renamed the same as a value, and that ordinary text and network names are left alone; the existing full-run budget test (part 5) is updated from 3 to 4 model calls, since "what should we post more of?" now correctly adds the strategist subagent its own fixture plan had left out. All named suites re-run: tests/agent.mjs 141, tests/insights.mjs 156, tests/observatory.mjs 40, tests/api-audit.mjs 157 checks (10 pre-existing notes, unrelated), tests/rooms.mjs 604, tests/no-dashes.mjs, all clean; tests/console2.mjs fails at the same Lantern-room-click timeout already found and left as pre-existing in part 5 (confirmed unrelated to every file this entry touches, by the same stash-and-rerun check).
2026-09-24 | site | edited | api/_agent.js, api/_llm.js, tests/agent.mjs | The Lantern agent, part 5: a real question run against production on the OpenRouter free tier (nvidia/nemotron-3-ultra-550b-a55b:free) found three live defects, all fixed. THE PLANNER could not be read: a free model's own reply carries a <think> block, a reasoning preamble, a code fence with trailing prose, single quotes, a trailing comma, or a bare array of steps instead of {steps:[...]}, none of which the old largestBraceRun (first "{" to last "}") could parse reliably. api/_agent.js's parsePlan now runs stripThink (drops a <think>...</think> block and anything before the first brace or bracket), a new extractBalanced (a real bracket-depth, string-aware scanner that finds the first balanced {...} or [...] span rather than guessing from the first and last brace in the whole text), stripFence, dropTrailingCommas, and, as the one repair attempt, singleToDoubleQuotes (a quoted-span-aware rewrite, not a blind apostrophe replace, so an apostrophe inside an already-correct string, "reel's own reach", is left alone); validatePlan now folds a tool or subagent name through foldName (lowercase, letters only) against TOOL_FOLD/ROLE_FOLD lookup maps, so "Reconcile Read" or "STRATEGIST" resolve the same as the canonical name. When parsing still fails, fallbackPlan(message) is no longer observatory alone: it reads the owner's own question for keywords (kind or subject or "what to post" adds insights and numbers; site, visitors or readers adds visitors; a named network adds numbers and reconcileRead for that network; package or draft adds package; the line-up or next week adds lineup and shelf), always opens with observatory and always closes with the analyst and strategist subagents. api/_llm.js's route() gained a task.json flag: Groq and Gemini always get response_format:{type:"json_object"}; OpenRouter only gets it for a model whose own /models entry lists response_format or structured_outputs in supported_parameters (orJsonCapableIds, a best-effort, fail-closed, six-hour-cached lookup, never overriding a caller's own response_format); this only ever improves the odds of a clean first parse, the robust parser above remains the real line of defense regardless of whether json mode was granted. THE COMPACT TOOL JSON dropped the very breakdown the question needed: observatory's and insights' own byKind and bySubject sit after large fields (trend30, weekdayHour) in JSON.stringify's own key order, so the old flat slice(0, 6000) cut them off before a model ever saw them, and the model then answered "the data does not break down reach by kind" when it did. compactToolOutputs' compactOne now rebuilds each tool's JSON by priority, not by truncation: cheap scalars and small objects first, then the named breakdowns in a fixed order (byKind, bySubject, byHour, byNetwork, kindTotals, top, subjectTop, subjectBottom, notes, sentences) each fit to its own remaining budget by fitToBudget (which keeps whole rows or keys, never cuts one in half), then everything else, the long tail, cut first when the budget runs out since it is assembled last. THE CRITIC left debris: dropping an invented clause from "(verse, dhikr, )" left the empty parenthetical sitting in the answer. checkAndClean's return now runs through a new tidyPunctuation, a five-pass fixed-point cleanup removing an empty bracket or parenthesis, an orphaned "and" or "or" before a closing bracket, a doubled comma, a trailing comma before a closing bracket or a period, and a doubled space, so "reel type (verse, dhikr, ). It only shows totals." now reads "reel type (verse, dhikr). It only shows totals." The synthesis prompt (SYNTH_SYSTEM) gained a direct instruction to answer using whatever breakdown the JSON actually carries and to say a breakdown is missing only when it truly is not in what was given, never as a way to answer an easier question than the one asked. tests/agent.mjs grown from 76 to 126 checks: a new block feeds each raw shape above (think block, reasoning preamble, nested JSON, single quotes, trailing comma, bare array, and all combined) into parsePlan and confirms validatePlan's name folding; a new block confirms fallbackPlan now chooses tools by the question's own keywords, always opens with observatory and closes with strategist, and never exceeds the step budget; a new block confirms the exact production debris string cleans correctly, alongside orphaned "and", empty-comma-only parentheticals, empty brackets and ordinary evidenced prose left untouched; a new block compacts an over-6000-character Observatory-shaped fixture and confirms byKind (with n on every row) and bySubject both survive while a long-tail field is trimmed. All three named suites re-run: tests/agent.mjs 126 passed, tests/llm.mjs 46 passed, tests/console2.mjs fails at its Lantern block (page.waitForSelector 15000ms exceeded clicking into the House room) both with and without this change (confirmed by stashing api/_agent.js, api/_llm.js and tests/agent.mjs and re-running against the unmodified tree), so the failure predates and is independent of this fix and was left as found, outside this entry's scope; the broader safety sweep (observatory.mjs, insights.mjs, api-audit.mjs, no-dashes.mjs, rooms.mjs, lantern.mjs, reconcile.mjs, social.mjs, jev.mjs) stayed green.
2026-09-24 | site | edited | api/_agent.js, api/lantern-agent.js, api/_llm.js, api/_playbook.js, api/social.js, api/observatory.js, admin2.html, vercel.json, OPERATIONS.md, tests/agent.mjs, tests/console2.mjs, tests/reconcile.mjs | The Lantern agent, part 4: a refuter's review found a proposal could be lost between the stream and the store, artifacts and a subagent's own gist never met the critic, the daily cap could fail open, an undo could restore the wrong prior value, thread memory was computed and never used, and the console still leaked raw ids and internal slugs; all fixed. THE PROPOSAL: api/lantern-agent.js's emit() now builds a proposal's id and saves it to nlan:proposals BEFORE sseWrite, not after runAgent returns, so a function killed mid-stream loses nothing; token now streams before every artifact, so a bad artifact can never cost the answer. THE CRITIC (api/_agent.js): rewritten to drop the WHOLE clause or sentence a bad number lives in, never splice a placeholder into half-true prose; checks percent/k/x-multiplier/times-word/ordinal/number-word forms, a number passing only if literal in this run's own evidence (built from the compact JSON actually sent to the model, evidenceFromCompact, not a wider set) or the ratio or percent-change of two evidence values of real size; the blanket x100/100 expansion and the plain-difference derivation were removed (both let almost any number pass on a large evidence set, the "birthday paradox" dens.mjs's own probe found), and a percent's literal check is its fraction form only, not the raw percent number against any unrelated count (the same over-permissiveness, found by re-running dens.mjs against a fixed critic). validateArtifact() is a real schema (type, row shape, size limits) that drops a malformed artifact with a note and never accepts a "proposal" type from a model at all, since a real proposal only ever comes from an action step's own buildProposal(); checkArtifact() runs the critic over every artifact's own strings and numbers, and over a subagent's gist before it streams. THE SCRUBBER (api/_llm.js): PHONE_RX rewritten to a real phone shape so it stops eating ISO dates and plain number runs; five more key shapes added (gsk_, AIza, ghp_, github_pat_, xox) with the `\b`-after-underscore boundary bug fixed; the Jev tool call in api/lantern-agent.js now routes through scrub() first and counts in the ten-call model budget, and the file's own comments about OpenRouter's ALLOW_PAID_MODELS were corrected to say plainly that this router never reads it. UNDO: api/social.js's teachGuard now hands its ledger entry the field's own exact prior value, whole ("date#slot" or null), not just the date half (an earlier cut of this truncated it, so an undo could restore a day that had never actually stood alone); a second undo of the same entry is refused both in api/_agent.js's undoAction and at api/lantern-agent.js's own door, and ledgerMarkUndone() writes the undone flag back with a real LSET. THE DAILY CAP fails closed: makeLedger()'s reserve()/release()/record() replace the old read-then-append pair with an atomic INCR, a store that cannot be read refuses rather than defaulting to zero taken today, and a failed action releases its own reserved slot. THREAD MEMORY: buildThreadContext() (the `recent` turns summariseThread() already computed, previously built and never handed to anything) now reaches both the planner and the synthesiser, so a follow-up question can be read against what was already said. MONEY DOCS: OPERATIONS.md's GROQ_API_KEY and GEMINI_API_KEY rows now say the key must come from an account with no billing enabled. BUDGET AND DEPLOY: wall time is checked before every model call, not only before a step, and a fallback is refused once under 12 seconds remain; max_tokens raised per call type (planner 600, subagent 900, synthesis 1800); vercel.json's maxDuration for api/lantern-agent.js raised to 150 to keep a margin over the 120-second run budget; recentChanges now fetches changes.txt from the public site with a 4-second timeout, failing soft, rather than reading the local file. THE CONSOLE (admin2.html): the room now loads lantProposals() on open and shows a persistent "Waiting for a yes or a no" panel, so a proposal survives a reload; the thread renders question, its thinking, its answer (in its own aria-live region), then its artifacts, oldest turn at the top, newest at the bottom, auto-scrolled with lantScrollBottom; a ledger row and a proposal's own header are drawn from a small humanizer (lantWhatText) rather than the raw action slug, and a proposal separates the planner's own Why from this run's own Evidence; dt() no longer throws on a row that was never an array, drawing it as its own single cell instead of crashing the whole table (caught by the refuter's own pw.mjs, which sends exactly that on purpose); overflow-wrap:anywhere added to every free-text surface. THE OBSERVATORY NOTE that called Instagram's own 1,200-person rise unitless, and compared an absolute delta across reach and views as though the two were the same measure, is rewritten to compare like with like, a percent change on its own stated base, the network named rather than a bare "the largest move". api/_playbook.js's comments at the small-n-caution principle and its own header were corrected to stop claiming the critic mechanically re-checks a folklore label or a five-post floor it never actually reads; those are asked of a subagent's own prompt, not this file. tests/agent.mjs rewritten for the new reserve/release/record ledger interface and whole-clause-drop critic behaviour, and grown with checks for the daily cap failing closed (no reserve(), a store that answers ok:false, a reserved slot released on failure), a second undo refused, artifact and gist validation through a full runAgent() run (a model-invented proposal artifact and a non-array table row both dropped, a real chart's own number kept), and thread memory actually reaching the planner and the synthesiser; Part B's in-memory store gained DECR and LSET, missing before, so release() and ledgerMarkUndone() were previously silent no-ops against the test store. tests/reconcile.mjs's own real, end-to-end teachGuard() run now also proves the exact prior value (day and slot) is what gets taught and what a real revertTaught() restores, byte for byte. tests/console2.mjs's Lantern fixture rebuilt closer to the real event shapes (a proposal's id, why, reason and evidence as separate fields; a ledger entry carrying args and an undone flag) and its assertions rewritten for the humanized ledger text, the Why/Evidence split, the aria-live answer and the oldest-top thread order. All named suites re-run clean: tests/agent.mjs 76, tests/llm.mjs 46, tests/observatory.mjs 33, tests/console2.mjs 806, tests/insights.mjs 146, tests/api-audit.mjs 157 checks (10 pre-existing notes, unrelated), tests/no-dashes.mjs, tests/rooms.mjs 604, tests/reconcile.mjs 88, tests/social.mjs 60, tests/lantern.mjs 25, tests/jev.mjs 18. The refuter's own probe scripts were re-run directly: p1.mjs's scrub and critic cases all match (the literal date and the genuinely derived ratio both kept, an invented percent dropped); e2e.mjs's stream now carries a real proposal id saved before it streams, drops the model-invented proposal artifact and the chart with invented numbers, and shows max_tokens 600/900/1800 were actually sent; gate.mjs's 24 owner-gate probes across all three routes answer 401 and never write to the response body; pw.mjs and pw2.mjs (run against a live server, screenshots taken at 390 and 1280) show no XSS anywhere, no side scroll, the correct DOM order, and the fixed dt() surviving a malformed row that used to crash the room outright. One residual limitation found and not fully closed: dens.mjs's own worst-case probe (the whole Observatory answer as one evidence blob, hundreds of numbers) still lets an invented percent through by coincidental percent-change derivation against an unrelated pair; the blanket-expansion and difference-check causes of the same probe's earlier, much larger failure are fixed, and evidenceFromCompact() keeps real evidence sets far smaller than this worst case in production, but a fully context-blind pairwise check cannot rule out every coincidence on an arbitrarily large evidence set without also refusing genuinely well-supported derived claims.
2026-09-24 | site | added | api/_playbook.js, api/_agent.js, api/lantern-agent.js, tests/agent.mjs | edited | api/social.js, vercel.json, admin2.html, tests/console2.mjs, OPERATIONS.md, ARCHITECTURE.md | The Lantern agent, part 3: an owner-only strategist in a new admin2 room, "The Lantern", that plans a request, reads the house's own read-only tools, hands parts of the reading to subagents on the router's free models, and answers with sourced numbers, a chart or a draft, never inventing a fact and never trusting a model to police its own honesty. api/_playbook.js is the strategist's knowledge: nine house rules (no dash, no invented fact, sources shown, no face) and seventeen principles drawn from published research on social platforms' own stated ranking factors, each one labelled exactly as the research marked it, verified primary, reported, or folklore, never blurred into one voice. api/_agent.js is a pure orchestrator, no model, no store, no network import, so it is provably testable: PLAN (a fast-tier call, capped at eight steps, ten model calls, 120 seconds), TOOLS (fourteen named read-only functions), SUBAGENTS (analyst, strategist, writer, critic, each one model call), SYNTHESIS (the answer plus artifacts). A mechanical critic runs on every answer regardless of budget: it strips any dash a model let through and replaces any number not found in this run's own tool output with "an unverified figure", logging the removal. api/lantern-agent.js wires this to the real router, the real tools (observatory.js, _insights.js, visitors.js, social.js's reconcile, _package.js, the Content Graph, page.js's shelf, _schedule.js's chooseReel, changes.txt, the search index, playbook, Jev), and the real store for a ledger and a thread; every tool output is run back through the router's own scrub() before a model sees it. Of three autonomous possibilities named in the design, two are wired to existing safe doors, refreshing the network numbers (api/insights.js) and teaching the duplicate guard from a reconciliation (api/social.js's teachGuard, paired with a new revertTaught() so the guard's hash can be written back to exactly what it held before); the third, reordering or skipping a reel within a day's unsent lineup, has no such door (api/overrides.js patches unrelated site copy, api/_schedule.js's pin is an internal repair path for an already-sent slot only), so it is always a proposal, never executed, and the agent says why in its own words. At most five autonomous actions run a day, each logged to nlan:actions with who, what, why, the state before and an undo recipe where one exists; a proposal always needs the owner's own Approve or Decline. admin2.html gains the Lantern room: a chat column, a collapsible thinking stream open while a run is in flight and folded once it ends, artifacts (charts and tables reusing the Observatory's own obsBars, trendSvgBig, dt and vizCard, a draft with Copy and a character count, a proposal with Approve and Decline), and an actions ledger with Undo, all reachable at 360px and with reduced motion honoured. tests/agent.mjs (56 checks, router, tools and store all stubbed, no network) proves plan parsing and repair, budget enforcement on both steps and wall time, the critic's evidence matching and dash stripping, per-person data never reaching a model, the daily action cap and its refusal into a proposal, a real undo that reverses a taught guard entry to its exact prior value, the SSE event order and format, the owner gate, and a proposal that needs a yes. tests/console2.mjs gains a dedicated Lantern block (stubbed SSE, no live model or store) proving the thinking stream opens and folds, a chart artifact's table toggle actually swaps the view, a proposal renders and Approve posts exactly the right id, the ledger lists a real action and its Undo posts the undo, and nothing scrolls sideways at 390 or 1280; the House hub's own room-count assertion is updated from eight rooms to nine. OPERATIONS.md gains the route in the API surface table, a full section on the agent's design and its two safe doors, and a runbook for a stuck proposal or a run that could not finish; ARCHITECTURE.md gains a section, "The Lantern agent". All named suites pass: tests/agent.mjs 56, tests/llm.mjs 46, tests/observatory.mjs 33, tests/console2.mjs 794, tests/insights.mjs 146, tests/api-audit.mjs 157 (10 pre-existing notes, unrelated), tests/no-dashes.mjs, tests/rooms.mjs 604.
2026-09-24 | site | added | api/_llm.js, api/lantern-models.js, tests/llm.mjs | edited | OPERATIONS.md, ARCHITECTURE.md | The Lantern's model router, part 2: two more free providers beside OpenRouter, so a bad day on one is not a dark Lantern. api/_llm.js speaks the same OpenAI-compatible chat shape to Groq (GROQ_API_KEY, an allow-list of four models confirmed free on console.groq.com/docs/rate-limits: openai/gpt-oss-120b, openai/gpt-oss-20b, openai/gpt-oss-safeguard-20b, qwen/qwen3.8-27b) and Gemini (GEMINI_API_KEY, an allow-list of the free 3.x Flash and Flash-Lite family from ai.google.dev/gemini-api/docs/pricing, 3.1 Pro Preview deliberately left off since it carries no free tier), each list intersected live against that provider's own /models endpoint before a name is ever tried, with no ALLOW_PAID_MODELS-style escape hatch for any of the three providers inside this router: OpenRouter's own variable of that name still exists and still works, but only inside api/_models.js's older chain, which the legacy callers (assistant.js, guide.js, marketing.js and the rest) use directly; this file's own copy of the same read is dead code that this router never consults. route({tier, messages}) walks three named orders (fast: Groq gpt-oss-20b, Gemini Flash-Lite, OpenRouter's best free; strong and long: Gemini's newest free Flash, Groq gpt-oss-120b, OpenRouter's best free), falls back across providers on error, 429, timeout or an empty reply, and remembers the model that last answered each tier (nllm:good:<tier>) to ask it first next time, the same idea as _models.js's nlm:good. A per-provider, per-model token bucket in the store keeps every provider conservatively under its published ceiling (Groq 25 RPM/800 RPD/150K tokens a day; Gemini 8 RPM/150 RPD, cautious since Google does not publish 3.x numbers; OpenRouter 15 RPM/40 RPD unless OPENROUTER_DAILY is set) and skips a model whose bucket is empty rather than asking it anyway; when every bucket for a tier is empty the caller is told plainly when the day resets in UTC, never left with a silent failure. scrub() runs on every outbound message before anything is sent: emails, phone numbers, IP addresses, Bearer/sk-/EAA/ya29-shaped tokens, long hex or base64 strings, and cookie values are redacted; any text carrying a Journal store-key marker (nj:e, nj:c, nj:list and the rest, or the literal [[journal]]) or a visitor_id/journal_id field is refused outright, never sent even redacted, since the Journal's anonymity is a wall, not a routing preference; a message marked perPerson is never sent to Gemini at all, since Gemini's free terms allow training on submitted content. api/_models.js is untouched, byte for byte: every existing caller (assistant.js, ask.js, guide.js, journal.js, marketing.js, social.js and the rest) keeps its exact current behaviour, and _llm.js reuses only its already-tested OpenRouter free-list logic (isFree, refreshFreeModels) rather than re-implementing it. api/lantern-models.js is the owner-gated window onto all of this (ownerGate, like api/insights.js): which of the three providers are configured (never the keys, only set/missing), the live free chain per tier, the week's usage against the budgets, the last good model per tier, and ?action=probe (POST): one tiny prompt through each tier, its latency, and which model answered. tests/llm.mjs (46 checks, fetch and the store both stubbed, no network and no Redis) proves free-only enforcement on all three providers including a priced model refused before any request would bill, the three tiers' provider order, fallback across providers on timeout, 429 and an empty reply, a rate bucket blocking a 26th call in one minute while a sibling model's own bucket stays open, a day's bucket resetting once the UTC date string changes, the scrubber catching every secret shape named above and refusing journal text and a visitor marker outright, per-person data routed away from Gemini while the walk still reaches a provider that may see it, a provider with no key simply absent from every chain rather than an error, and every provider exhausted naming the exact UTC reset time rather than failing silently. OPERATIONS.md gains the three variables (GROQ_API_KEY, GEMINI_API_KEY, OPENROUTER_DAILY) and the new route in the API surface table; ARCHITECTURE.md gains a short section, "The Lantern's models". All named suites pass: tests/llm.mjs 46, tests/lantern.mjs 25, tests/api-audit.mjs 148 checks (10 pre-existing notes, unrelated, none new for the two added files), tests/no-dashes.mjs.
2026-09-24 | api | added | api/observatory.js, tests/observatory.mjs | edited | api/_insights.js, api/social.js, api/visitors.js, vercel.json | The Observatory: one owner-gated call that composes every number the house already keeps into the shape a single room can draw a pattern from, so a reader never has to hold six rooms' worth of numbers in their head at once to notice one. api/observatory.js is owner-gated exactly like api/insights.js and api/house.js (ownerGate), GET only, Cache-Control no-store, and its own composed answer is kept ten minutes under nsoc:observatory:v1 (?fresh=1 goes round it, the same door /api/house already opens) to protect the free store and API tiers from a room left open. It calls existing module functions directly rather than recomputing: numbers() (api/_insights.js) for the week-over-week folds by network, kind, weekday and slot; computeVisitors() (api/visitors.js, newly exported, the handler's own body pulled out unchanged so the room can read the same site numbers without a second network round trip or a third hand-rolled copy of the store-speaking block the file's own header already explains is deliberately kept to two); postedChannelCounts() (api/social.js, new, one HGETALL of the existing nsoc:reels:postedch hash the duplicate guard already writes, counting distinct reels per channel, nothing written). Three things nothing else in the house had computed yet are built here, from the stored snapshots and slot records alone, no network call: a single walk of thirty days of nsoc:stats:<date>#<slot> (one MGET) folds into the per-network daily trend, a weekday by hour matrix of Instagram's median reach and YouTube's median views (matchedCard, newly exported from api/_insights.js, pairs each snapshot with its card so subjectOf can bucket it), and the per-kind and per-subject totals over the full window rather than the fortnight numbers() keeps; a second walk of the same thirty days' raw slot records (readSlot, chunked ten at a time the way collect() already is) gives posting health per day, sent, half sent, failed, a channel retried (tries > 1) and a duplicate the guard caught (dupWarn or already), the one walk that still counts a slot nothing answered, which the snapshot walk alone cannot see. Library coverage is read from the shipped Content Graph derivatives only, all local file reads (reels/index.json for the shelf as it stands, assets/reel-sources.json for how many Lights, words, Names, days and du'as have a reel, CONTENT_STATUS.md's generated graph-counts block, parsed rather than hand-copied, for each type's own total); every ratio compares a corpus against its own total, never a shared cross-row figure. Missing data is null throughout, never a manufactured zero: a day with no snapshot at all is absent from the trend, a real post that reached nobody keeps its true zero. vercel.json's api/observatory.js gains maxDuration 60 (matching insights.js and house.js, no live network call so well inside it) and includeFiles for reels/index.json, assets/reel-sources.json and CONTENT_STATUS.md (assets/reel-subjects.json already rides the api/_insights.js import). tests/observatory.mjs (31 checks): the owner gate (no secret 501, no cookie 401, the console's own signed cookie through), an explicit zero reach staying zero against a day with nothing recorded reading null, the 30-day trend and summary band, the weekday by hour matrix carrying every weekday even where an hour has nothing and only the hours actually used as columns, the 5-post floor keeping a thin kind in the answer rather than dropping it, posting health's retry and duplicate counts read straight off fixed slot records, the library ratios never invented, and the ten-minute cache genuinely not recomposing. All named suites re-run clean after the shared-file edits: tests/insights.mjs 146, tests/social.mjs 60, tests/house.mjs 156, tests/traffic.mjs 70, tests/api-audit.mjs 146 checks (10 pre-existing notes, unrelated), tests/no-dashes.mjs.
2026-09-24 | console | edited | api/observatory.js, admin2.html, tests/observatory.mjs, tests/console2.mjs | A second look at the Observatory's own first screenshot found five real faults, all fixed. FIRST, jargon: patternNotes' best/worst-slot sentence read "The reelA slot engages best this week and reelE least", an internal slot id the owner has never seen anywhere else in this console. api/observatory.js now carries SLOT_NOUN, a fixed name for each of the eleven slots (the five card slots by their own word, the six reel slots by the half of the day they share, api/_schedule.js's own HALVES: morning, noon, afternoon, evening, late, night reel) and slotPhrase(), which turns a bySlot row into "19:00 UTC (a late reel)"; the sentence now reads "The 19:00 UTC post (a late reel) engages best this week, and the 09:00 UTC post (a coming-up card) least," no id anywhere. admin2.html's own shelf-coverage bars carried the same smell at one remove, reels/index.json's plain lowercase kind field ("verse", "short") shown raw; OBS_TYPE_LABEL now names every one the library section already names its corpus by. SECOND, the heatmap showed three or however many hours happened to have a number that month as its columns, discovered from the data rather than the schedule; HEAT_HOURS is now read straight from api/_schedule.js's own SLOTS (08, 11, 14, 17, 19, 21 UTC, the six reel hours; the five card slots keep fixed times that are not a choice being made) and weekdayHour always emits all six on every weekday, present or not, forty-two cells rather than however many the data happened to fill; an hour nothing was posted at reads null and the room marks it with a dashed border and "no post recorded" rather than leaving no column at all. THIRD, the summary band: eight tiles (four headline numbers, four per-network) shared one six-wide grid and wrapped into two uneven rows with orphans at 1280 and a bare "Threads ·" card. Recomposed into a hero row (.otiles, reach, views, site visitors, posts sent, four tiles that never share a grid with anything else) and a new .scoreboard below it, one .srow a network in a fixed column order (name, posts, reach or views, engagement, the week's move, the 30-day line); a network with nothing recorded, summary.networks now honestly carrying null rather than a stray 0 for Threads, renders one line, "No numbers yet," instead of a row of dashes. FOURTH, the library meters: "304 of 350" wrapped onto three lines in a 40px value column sized for a plain bar's number. obsMeters' wrapper now carries a `meter` modifier (.bars.meter .b, a 130px value column, tabular numbers) and the count and the percent sit beside each other, one line, "304 of 350   87%". FIFTH, general legibility: the four-network small multiples (the 30-day trend and the funnel) dropped their own nested background-and-border box, four card-shaped panels sitting inside one card that already had a border, in favour of a hairline top rule, since a viz card holding four more cards inside it was the "card in card" look the plainest to see and the easiest to stop doing; the trend panels grew from 64px to 96px tall (the SVG's own viewBox height corrected to match, since the old 64-viewBox-into-96-CSS-pixel mismatch, caught while making this very change, was stretching every end dot into an ellipse) and now label both the endpoint ("445 now") and, where it differs from the endpoint, the month's peak ("22 peak"); a new placeLabel() works out anchor and position from the label's own character count against the fixed-width mono face rather than the point's raw index, which a first pass got wrong for YouTube's own sparse five-post month (the label read "1,740 n", clipped, because a point well short of the viewBox's true right edge was still treated as if it were flush against it). tests/console2.mjs's OBS fixture was rebuilt to match what the store really holds: Facebook now carries its own eleven-post-a-day curve at real numbers (reach in the hundreds, not Instagram's own borrowed curve, the two having shared one generator before), Threads carries null throughout rather than a stray zero, the weekday-by-hour fixture carries all six reel hours, and the notes array carries the same slot-phrase and duplicate-catch sentences the real code now writes. tests/observatory.mjs's weekday-by-hour section rewritten for the fixed six-hour columns (forty-two cells, an untouched hour reading null with instagramN 0 rather than missing its column); 33 checks, up from 31. All named suites re-run clean: tests/observatory.mjs 33, tests/console2.mjs 744, tests/insights.mjs 146, tests/traffic.mjs 70, tests/api-audit.mjs 146 checks (10 pre-existing notes, unrelated), tests/no-dashes.mjs. Screenshots retaken at both widths: tests/shots/console2-390-observatory-full.png, tests/shots/console2-1280-observatory-full.png.
2026-09-24 | console | added | tests/observatory.mjs (see above) | edited | admin2.html, tests/console2.mjs, OPERATIONS.md | The Observatory room, reached from House the same way System and Controls are (an eighth card on the hub, in the same list, same live-number convention). Design followed the dataviz skill's own procedure before a line was drawn: the console's dark surface (--ink #0B0D16, --panel #11131F) and its existing four-hue set (gold, good, bad, blue) were run through scripts/validate_palette.js first and failed the categorical six checks outright (lightness band, chroma floor and both CVD floors all failed at once), so the room is deliberately built to need no categorical palette at all rather than inventing one under time pressure: every multi-network picture is small multiples in the one gold the rest of this console already wears for "the series that matters" (the network's name is the label, never its hue), the weekday by hour grid is one validated sequential blue ramp (four steps, node scripts/validate_palette.js "#b7d3f6,#6da7ec,#2a78d6,#184f95" --ordinal --mode dark --surface "#161A2A": lightness monotone, every adjacent step >=0.06 of OKLCH L apart, the darkest step still 2.13:1 on the card's own background), and the only colour that carries a meaning of its own is the fixed sent/failed/waiting set the state chips already use everywhere else in this file, run through the same validator's contrast() export (gold 10.64:1, good 9.91:1, bad 7.16:1 against the panel surface). Every chart: a table-view toggle (a real <table>, every value reachable with no hover at all), a tooltip on hover and on keyboard focus alike (one delegated listener, a transparent hit circle on every trend point, a tab stop on every mark), direct labels on the four network trend panels (the room's whole "up to four" budget, spent there on purpose), a legend wherever two or more things share a picture (the heatmap's scale, the calendar strip's five states), no dual axis anywhere (a network's own reach and views never share a plot; a heterogeneous library coverage picture is a meter, each row its own percentage of its own total, not a bar scaled against the largest corpus, caught and fixed after the first screenshot made Names' full 99-of-99 coverage look nearly empty beside Verses' 2,981). Summary band (this week against last: reach, views, site visitors, posts sent, each with a sparkline and a delta chip, then one tile a network); three to five plain sentences the server computed (api/observatory.js's own patternNotes, read here, never recomputed); the funnel, post to reach to engaged to a site visit, one small panel a network; thirty days, one line-and-area panel a network; the weekday by hour heatmap, Instagram reach and YouTube views; kind and subject bars, the 5-post floor shown as a faded row rather than a hidden one; the site (visitors, by country, by room, by source); posting health as a 30-day calendar strip, a square a day, coloured by state, retried and duplicate counts on hover; library coverage, the shelf remaining, and reels posted by network. Stacks to one column below 640px, a chart's own overflow-x:auto lets a wide heatmap scroll inside its own card rather than the page, respects prefers-reduced-motion (the trend panels' hover lift and the existing .bars width transition both guarded). tests/console2.mjs: an OBS fixture in the real composed shape, routed at /api/observatory, Observatory added to the ROOMS loop (opens from House, the hash, the title, the back affordance, no side scroll, a reload lands back in it) and its own dedicated block, every chart carries its own table toggle, pressing one swaps the chart for a real table and back, a tooltip target is a tab stop and opens on hover and closes when the pointer moves away, no console error or failed request while it drew, screenshots at 390 and 1280. The pre-existing hub-order assertion, written for seven rooms, is updated to eight with Observatory last. OPERATIONS.md's API surface table gains the route and section 7 gains the test line. All named suites pass: tests/observatory.mjs 31, tests/console2.mjs 744, tests/insights.mjs 146, tests/traffic.mjs 70, tests/api-audit.mjs 146 checks (10 pre-existing notes, unrelated), tests/no-dashes.mjs. Screenshots: tests/shots/console2-390-observatory-full.png, tests/shots/console2-1280-observatory-full.png (also -390-observatory.png/-1280-observatory.png from the generic room loop). One known, pre-existing artifact, not introduced here and not a rendering fault: Playwright's fullPage screenshot mode duplicates a position:sticky or position:fixed element (this shell's own header bar) mid-page when the page is taller than one viewport, visible in the Flow room's own pre-existing screenshot the same way; a real browser never shows it, only the stitched PNG does.

2026-09-24 | console | edited | admin2.html | The first live read after the Threads key: every verse reel is its own surah, one post each, so the subject fold said "Surah 19 (Maryam) leads" on the strength of one post. A subject is now ranked only once it has five posts, the floor the sentences already keep, and the fold says how many subjects are still too thin to rank.
2026-09-24 | site | edited | api/page.js, api/_package.js, vercel.json, tests/package.mjs | Seen live: /api/package?id=verse:19-36, the best-performing post of the fortnight, said "No English meaning is held locally for this verse" though its own reel already carries the English. verseNotes' own .sense (verse/<s>.json) was the only local English source buildVerse read; the shelf's 600 verses carry their Saheeh meaning too, but in a file (tools/reels/verses.json) no function had read before, not in tools/reels/verses.txt (already in api/package.js's includeFiles, but its own comment behind each ref is a short paraphrase hook, not the reel's real translation) and not in reels/index.json's caption (the meaning sits unstructured inside the prose, never a field on its own). api/page.js gains shelfVerseMeaning(s, a) and shelfVerseEdition(), reading tools/reels/verses.json the same once()-memoized way quranTable() reads the Uthmani text. buildVerse (api/_package.js) now falls to the shelf's own wording, labelled the way the reel labels it (TRANSLATION_LABEL, "Saheeh International" off api/page.js's own n2-credit line, keyed by the file's own edition field) only where verseNotes holds nothing; a multi-verse range only takes the shelf's word when every verse in it has one, never half a passage read as whole. Draft is Arabic plus the reference only when neither source holds a meaning, unchanged from before. vercel.json's api/package.js includeFiles swaps tools/reels/verses.txt for tools/reels/verses.json (checked first: no function this route can reach, verseRows/shelfRefs, is on api/package.js's call path at all, only api/page.js's own verse-room and sitemap read it, so the swap costs nothing and keeps the glob at 240 of 256 characters). tests/package.mjs: verse:19-36 now carries the shelf's own Saheeh wording and the "Saheeh International" label, no English-meaning gap named, the Instagram draft carries it too; verse:2-1, on neither the shelf nor the local notes, still honestly carries no English text and still names the gap. All three named suites pass: tests/package.mjs 285, tests/api-audit.mjs 143 checks (10 pre-existing notes, unrelated), tests/no-dashes.mjs; tests/rooms.mjs also rerun as a check on the vercel.json edit, 604.
2026-09-24 | site | edited | api/_package.js, api/package.js, admin2.html, tests/package.mjs, tests/console2.mjs, OPERATIONS.md | added | scripts/graph/derive_reel_sources.py, assets/reel-sources.json | Two refuter passes on the Content Factory (masterplan step 6), fixed before review; this line replaces and corrects the one it superseded. FIRST PASS, derivatives now read the graph, not a guess: the old derivativesOf matched a reel or a film to an object only where the shelf's own id pattern happened to say so, finding zero of the 225 Did-you-know cards and only the two Hajj words' films by a hard-coded exception. scripts/graph/derive_reel_sources.py (wired into scripts/graph/run.sh as its seventh step) reads the Content Graph's own reel_of edges and writes assets/reel-sources.json (180,059 bytes: 1,589 content objects, 1,565 reel edges, 35 film edges, each with its own confidence, plus a `names` map, a Name's 1-99 position to the graph's own content_id); api/_package.js's derivativesOf() and contentId() read it. Corrected here, not claimed before: 30 of those 35 film edges trace to a `hero:` or `page:` target, a type this route has no way to be asked about (only its ten room types resolve), so only 5 films, the ones traced to a Light or a word, are ever reachable through a package this route can actually build; OPERATIONS.md now says so. SECOND PASS, the refuter's own report: (1) THE X THREAD CHANGED THE OBJECT'S WORDS. sentencesOf split on every . ! or ?, turning "29.53" into "29." and "53", and stranding a closing quote at the front of the next post ("...in Jannah." became a post starting '" He answered'), on 139 of 1,324 objects. Rewritten to a real boundary: terminal punctuation, plus whatever closing quote or bracket belongs to it, stays on the sentence that is ending; a split only happens where that is then followed by whitespace and a sentence actually starting (a capital letter, an opening bracket, or an opening quote not itself quoting a bare number), so a decimal and a lowercase continuation after a quoted question both stay one sentence. tests/package.mjs's own check no longer re-splits the body to check itself: it collapses whitespace and compares the joined posts, minus the numbering and the notice, character for character against the object's own text, over the whole corpus, 1,907 objects, not a sample (0 mismatches, 145 threads honestly truncated at a sentence boundary). (2) A SURAH CITED A SOURCE THAT DID NOT BACK ITS OWN TEXT. study/<n>.json's `virtue` is a hadith about the merit of reciting a surah; buildSurah's drafted text is `context` or `name_story`, the house's own written account of the surah, never the virtue report. Citing virtue.src beside that text printed "Source: no authentic virtue report..." on 17 surahs and a real but unrelated hadith on 44 more; a surah now carries no source line at all, since its paragraph is the house's own account from the commentators, and its gaps say no source is recorded for it. The same mistake, corpus wide: sourcesOf() (a prophet, a companion, a character, a chapter of the Path) kept a hadith's source even where the fact it documents never appears in the drafted text (Adam's story never says he was created on a Friday, though a hadith on his record does); a hadith's source is now kept only when a four-word run of its own text is actually found in the body about to be quoted (21 of a prophet's 26 hadith citations were unbacked this way, 9 of 54 on a companion or a character, 37 of 169 on a chapter). Every Qur'an ref stays, the same record's own curated verse. (3) A VERSE WITH NO WRITTEN NOTE PRODUCED AN EMPTY DRAFT REPEATING ONLY THE TITLE. buildVerse now always carries the Arabic (detail), and the sense only when verseNotes holds one locally (text, leading when present); gapsFor names "No English meaning is held locally for this verse." honestly rather than leaving the draft empty. (4) DICTIONARY AND CONSOLE HARDENING. buildWord checks the dictionary's own key (hasOwnProperty) before answering, so word:__proto__, word:constructor and word:toString refuse (404) rather than answer from the prototype chain; buildName accepts name:<slug>, the id the package itself prints, by reversing assets/reel-sources.json's own map; a decodeURIComponent that cannot read a site-path id's %-escape is caught and refused (400) rather than crashing the route (500); an id over 200 characters is refused (400) before it is processed; no error echoes more than a short prefix of what was sent. admin2.html's pkgGo() now refuses to fire a second request while one is in flight (Enter bypassed the disabled button's own click guard), and the Pinterest description's own destination link, never carried in the description text, is now shown and labelled beside it. (5) THREADS DROPPED THE SOURCE LINE ON EVERY SOURCED OBJECT, UNCONDITIONALLY, since api/_threads.js's own card path takes only the first sentence of whatever body it is given and never reads a source field; fixed without touching _threads.js, entirely in the body api/_package.js builds for Threads alone: the source gets first claim on the room (word boundary, ellipsis, if it alone does not fit) and the lead sentence whatever is left, so _threads.js's own oneLine() reads the two as one sentence and both survive; proven corpus wide, 1,129 of 1,129 sourced objects now keep their Threads Source line, 0 over 500 characters. tests/package.mjs rewritten throughout with the fixes above, all corpus wide: the splitter-independent thread check; the "no silent loss" sweep for Pinterest and Threads, now also proving the Threads Source-line fix; a surah source-honesty sweep, all 114; a hadith-tied-to-text sweep naming Adam's two unbacked hadith; a verse with and without a note; the five prototype-chain refusals; the Name-slug round trip, including the Al-Majid/Al-Maajid pair; malformed, oversized and decode-failing ids, checked for their exact status code at both buildPackage() and the route. tests/console2.mjs gains the in-flight Enter guard (three fast presses, one request) and the Pinterest link display. OPERATIONS.md's route row documents every accepted id form, the 200-character cap, the decode and prototype-chain refusals, and the 5-of-35-films caveat. All named suites pass: tests/package.mjs 281, tests/content-graph.mjs 30 (untouched, confirming this batch touches no graph file), tests/rooms.mjs 604, tests/api-audit.mjs 143 checks (10 pre-existing notes, unrelated), tests/no-dashes.mjs, tests/console2.mjs 706.
2026-09-24 | site | added | api/_package.js, api/package.js, tests/package.mjs | Masterplan step 6, in the owner's narrow form: one library object becomes a full package of per-platform drafts, on command, owner only, nothing posted, no bulk run, no paid compute, no model. api/_package.js resolves a content_id or a site path (/light/<id>, verse:2-255, /dictionary/<id>, /prophet/<id>, /companion/c-<id>, /character/<id>, /place/p-<id>, /path/<n>, /surah/<n>, /name/<n>) through api/page.js's own loaders (lightById, parseRef, localArabic, verseNotes, dictionary, prophetById, characterById, placeById, chapter, study, surahRow, nameRow, manifest, verseRows, relatedLights, chapters; edgesOf newly exported there, a one-line addition, no behaviour changed) and returns the object's own title, text, sources and Arabic exactly as the data holds them, its derivatives (a reel card by exact id match on the shelf; a film only where the fact is certain and cheap, the Hajj words hajj.html#ihram and hajj.html#miqat trace to word:ihram and word:miqat the same way ARCHITECTURE.md documents build_graph.py tracing them, nothing fuzzier attempted since the full content graph is git ignored and never deployed; related rooms from entity-graph.json's edgesOf, relatedLights, or a chapter's/surah's own connections field), plain-sentence gaps (no reel, no film, no sources, no Arabic, no related rooms, whichever is true), and one draft per platform built deterministically from those same words: YouTube (title capped 100 via _youtube.js's own titlePlain, no forced #Shorts since no video exists yet; description via YT.shape()), Instagram and Facebook (api/_channels.js's own shape(), hook/body/sources/link/tags), Threads and Pinterest (the same shape(), reusing _threads.js's 500-char card rule and cutKeepTail), Reddit (shape()'s own reddit branch: sources inline, the link only at the end, no hashtags), and an X thread built here (none existed to reuse): the object's title as the hook, its sentences packed at 264 characters so a "k/n " prefix never crosses 280, a final post carrying the sources and the link with the sources cut short before the link is ever touched. Every text pulled from the data is sanitised (a dash becomes a comma, collapsing whitespace the way _channels.js's own cut() does) before it reaches a draft. Found while proving this across the whole corpus (1,314 objects: 350 Lights, 523 words, 25 prophets, 98 companions and characters, 34 places, 71 chapters, 114 surahs, 99 Names): Facebook and Instagram's generic branch and YouTube's own hard slice both cut a caption from the end once it is over the limit, which silently dropped the closing source and link lines for anything with a long detail (a prophet's whole story, a long Path chapter's hadith list); fixed by capping the body handed to each fixed-limit network in advance (word-boundary, ellipsis, the same convention as page.js's own clip()) rather than changing any network's own shaping rule. Re-swept the same 1,314 objects after the fix: zero platform-limit violations, zero missing links, zero em or en dashes anywhere in a package. api/package.js is the owner-gated route (ownerGate from api/_owner.js, the same as api/insights.js): GET ?id=, 400 with no id, 404 for an object that does not resolve, Cache-Control no-store. vercel.json's api/package.js carries the same includeFiles string as api/page.js (239 of 256 characters; reels/index.json and the graph's derived files already ride assets/*.json, so nothing new was added to the glob); .vercelignore's own documentation of what each function opens notes the new reader. admin2.html gains a "Make a package" card in the Marketing room (the room that already calls itself "the outreach studio"), between "Write my week" and the pipeline: an address field, a button, Enter-to-submit, and the result drawn as the object, its derivatives, its gaps, and one card per platform with a character count against that platform's own limit and a Copy button (the same paste-and-post habit the rest of the room already uses); reuses .card/.msg/.acts/.btn, no new colour. tests/package.mjs: one of each of the nine room families resolves; every draft within its platform's limit; no U+2013/U+2014 anywhere in a package; the canonical link on every draft; no source quoted that is not in the object's own sources array; the X thread numbered and every post within 280; the Reddit body carrying its sources before the link; gaps correct for an object with and without a reel; the Hajj word film trace; both input forms (a site path and a content_id) resolve the same object; the owner gate refuses with no ADMIN_SECRET (501), refuses with no cookie and no key (401), answers with the header key and with the console's own signed cookie, and refuses a request with no ?id= (400); 242 checks. tests/console2.mjs extended with a stubbed /api/package and ten new assertions per viewport (the card exists, the id typed reaches the query exactly, the object and its content_id are shown, the gaps and a related room are shown, one block per platform is drawn, each block's own limit is shown, the first block's Copy button copies only that block's text, an unresolved address is told so plainly, Enter in the field works the same as the button and an empty field is refused before it asks the house). OPERATIONS.md's API surface table gains the route. All named suites pass: tests/package.mjs 242, tests/rooms.mjs 604, tests/api-audit.mjs 143 (10 pre-existing notes, unrelated), tests/no-dashes.mjs, tests/content-graph.mjs 22 (unaffected, confirming this change touches no graph file), tests/console2.mjs 702.
2026-09-24 | content | added | scripts/graph/derive_person_words.py, assets/person-words.json | Generates the one file api/page.js's relatedWords() actually reads to know a dictionary word (khadijah, fatimah, mahdi and 25 more) is curated as the same record as a companion, a place or a figure (scripts/graph/same-as.json's own word: pairs), since same-as.json itself never deploys (.vercelignore's /scripts/) and the live site was silently missing the exemption though every local render showed it working; see the edit below for the fault and the fix. Wired into scripts/graph/run.sh as its fifth step; rides api/page.js's existing assets/*.json includeFiles glob, no vercel.json change needed. 28 words today.
2026-09-24 | site | edited | api/page.js, api/_channels.js, admin2.html, scripts/graph/run.sh, tests/rooms.mjs, tests/content-graph.mjs | Seen live: /companion/c-hasan showed the dictionary word "Hasan" (the hadith grade) because the companion is named al-Hasan, and /place/p-makkah showed "Sahih" because its account quotes a companion's words with the citation "(Tirmidhi 3925, sahih)". relatedWords() (api/page.js) now strips a citation before matching (a collection name with its number, a bare collection name next to a grading note, "Sahih al-Bukhari"/"Sahih Muslim" named outright, a verse reference) and drops a matched key whose every sighting reads as part of a person's name: preceded by ibn/bin/bint/abu/umm, followed by ibn/bin/bint, preceded by al- when the key is a given name a prophet or a companion actually carries (al-Masjid al-Haram stays a place, since no one in the library is named Haram), or, for a hadith grade or a fiqh term, a capitalised "X of Y" mid sentence (Yusuf's chapter no longer shows Aziz, the rare hadith, for "Aziz of Egypt") unless Y is a collection's own compiler (Sunan of Abu Dawud keeps its word). A word scripts/graph/same-as.json already curates as the same record as a companion, a place or a figure (khadijah, fatimah, mahdi and 25 more) is exempt from that guard entirely, so a Light that writes "Khadijah bint Khuwaylid" or "Fatimah bint Muhammad" keeps her own word. On an entity room besideEntity() also refuses to call the room's own name a related word, unless the dictionary entry really is that entity (entity-graph.json's own `word` field) or the shared token is not a technical word and not a given name a prophet or a companion carries: a refuter's review found the first cut of this too wide and rendered all 1,270 rooms to prove it (30 changed against a plain relatedWords() scan that skipped the self guard entirely, which is not what a reader sees); six rooms had lost the very word their own title names, not a look-alike (p-badr and a-badr lost Badr, Mount Uhud lost Uhud, Mina lost Jamarat, the Prophet's Mosque lost Rawdah, Iblis lost his other name Shaytan, Hamalat al-Arsh lost the Throne it carries), while Umm Ayman's own room correctly kept losing "barakah" for her own given name, Barakah, and the Names page correctly kept losing Al-Aziz, Al-Hafiz, Ash-Shahid, Al-Ahad and Al-Jami for the unrelated Hadith terms of the same spelling. The guard now fires only for a technical category (Hadith, Law and life) or a given name the prophets or companions carry, which keeps every one of those and restores the six. Also fixed: personWords() read scripts/graph/same-as.json directly, which never deploys (.vercelignore's /scripts/), so the exemption above was silently empty on the live site and Khadijah and Fatimah were losing their own word there though every local render showed them keeping it. scripts/graph/derive_person_words.py (new) writes assets/person-words.json (28 words) from same-as.json's own word: pairs, wired into scripts/graph/run.sh as its fifth step; api/page.js's personWords() now reads that file, which rides the same assets/*.json glob api/page.js's includeFiles already carries, so no vercel.json change was needed. Proved by copying the tree without scripts/ and rendering "Khadijah bint Khuwaylid" and "Fatimah bint Muhammad" from the copy: both still carry their own dictionary word. tests/content-graph.mjs gains a drift check, assets/person-words.json byte-identical to what derive_person_words.py produces from same-as.json right now; tests/rooms.mjs gains a check that the file exists and is not excluded by .vercelignore. Re-rendered all 1,270 rooms Lights (350), chapters (71), verses (593), prophets (25), companions (58), characters (40), places (34) and the 99 Names, through the real render(), against /root/noorH: 23 rooms changed. 19 by removal only, all checked against source text and confirmed a citation or a name look-alike (hasan x4, sahih x9, sahih-muslim x3, sahih-al-bukhari x1, aziz x2, hafiz x1, shahid x1, ahad x1, jami x1, munkar x1, haram x1, barakah x1, qadi x1). 4 also gained a word that had been crowded off the shelf's first 8 by the one removed and was already a true match: c-ali gained Tabuk, Umm Ayman gained Uhud, c-hasan gained Ummah, p-makkah gained Hajj. A Light that genuinely discusses hadith grading (bukhari-sahih-870, "his Sahih contains roughly 7,275 reports") still reads beside the word Sahih. A known, separate look-alike found and left alone: fold() drops the apostrophe from both "Mu'tah" (the battle) and "Mut'ah" (the fiqh word), so three companions' rooms (Khalid, Zayd, Ja'far) still show Mut'ah for a mention of the battle; fixing it needs fold() to keep apostrophe position, a larger change outside this order's scope, reported to the Director. Also fixed, api/page.js's parseRef(): a verse range label ("2:255" + en dash + "257") used the house's forbidden mark; now a plain hyphen, matching the reel titles (14:40-41). Grepped api/ and the static html for any other U+2013/U+2014 reaching a reader: api/_channels.js's Reddit-tab status string and five strings in admin2.html (the diagnostic list, the traffic breakdown, the store status line, a report's section eyebrow, the Islam-map's verse credit) carried one each; all now a colon or "·", matching the house's own convention elsewhere in the same files. Comments and an LLM prompt carrying a dash (api/social.js, api/_lights.js) were left alone, since neither reaches a reader; .build-src/characters.src.html, a build source file superseded by characters.js, was found carrying dashes too but is not served live and was left for a separate pass. tests/rooms.mjs's range-label assertions updated to the hyphen form; assertions added for c-hasan (no Hasan), p-makkah (no Sahih, both still carry their own dictionary word), bukhari-sahih-870 (still reads beside Sahih), and the two above. All four named suites pass: tests/rooms.mjs 604, tests/no-dashes.mjs, tests/content-graph.mjs 19, tests/api-audit.mjs 139 checks (10 pre-existing notes, unrelated).
2026-09-24 | social | edited | api/_insights.js, api/_threads.js, tests/insights.mjs, tests/threads.mjs | Masterplan step 8, finishing the learning loop, part one: Threads insights. api/_threads.js exports `token()`, the same thin accessor social.js already gives igToken() and pageToken(). api/_insights.js's fetchThreads() reads GET https://graph.threads.net/v1.0/{media-id}/insights?metric=views,likes,replies,reposts,quotes,shares (Meta's Threads API documentation, developers.facebook.com/docs/threads/insights, which also names views and shares "in development"): views stand in for reach, since Threads gives no separate reach metric; replies read as comments; reposts and quotes are kept beside shares rather than folded into it. A token missing threads_manage_insights answers the same code 10 or 200 shape Instagram's permission check already reads, so permissionMissing() catches it with no new regex. A refuter's review caught that api/_threads.js's own SCOPE, the permission list /api/threads?action=auth actually asks Meta for, never included threads_manage_insights, so telling the owner to simply re-authorise could never have granted it: Meta only grants what SCOPE requests. SCOPE now reads threads_basic,threads_content_publish,threads_manage_insights, and refresh()'s own message says plainly that opening /api/threads?action=auth again will make Meta ask him to approve the new insights permission, then to paste the fresh TH_TOKEN into Vercel. collect() reads a threads id off the slot record the same way it reads the other three; snapshot() and numbers() fold it in (NET_LABEL, missingToken); aggregate()'s byNetwork carries its own row. tests/insights.mjs stubs graph.threads.net and adds a dedicated section: the six names, the permission stop, collect() and refresh() picking up a Threads id, the cache key shape, and the batch-stop message naming TH_TOKEN. tests/threads.mjs's own SCOPE assertion updated to the three-scope string. tests/insights.mjs: 146 checks pass; tests/threads.mjs: 126 checks pass.
2026-09-24 | social | edited | api/_insights.js, tests/insights.mjs, tests/content-graph.mjs, vercel.json, .vercelignore, admin2.html | added | scripts/graph/derive_reel_subjects.py, assets/reel-subjects.json | Masterplan step 8 part two: the subject fold, corrected. The first version had api/_insights.js import api/page.js's groupOf(), groupLabel(), dictionary(), surahRow() and lightById() -- all five already existed there, exported for the room renderer, before this order; nothing was added to api/page.js for the subject fold, and this line previously said otherwise, which was false -- to read lights/all.json, assets/dict-index.json and tools/reels/quran-uthmani.json live. A refuter's review found the deploy gap this made: none of api/insights.js, api/house.js (through api/_flow.js's collect()) or api/warm.js carried includeFiles for those source files, so on Vercel the subject fold would ship silently empty, no error, nothing to say. Fixed the way assets/person-words.json already fixed relatedWords()'s own version of this bug: scripts/graph/derive_reel_subjects.py (new, wired into scripts/graph/run.sh as its sixth step) computes, at build time, every reel id's {group, label} for the four subjects a reel can carry (reel:light by the Light's own first-shared-tag grouping, ported from page.js's groupOf; reel:verse by its surah, named from the reels' own Qur'an table; reel:word by its dictionary category; reel:short, a silent film, by the field after #f- in its room) and writes assets/reel-subjects.json. api/_insights.js's subjectOf() now reads only that file, cached once per run, and no longer imports api/page.js at all. vercel.json's api/insights.js, api/house.js and api/warm.js function blocks gain includeFiles: assets/reel-subjects.json (api/social.js checked and needs nothing, it never reaches subjectOf). .vercelignore's own documentation block of every path a serverless function opens gains this file. tests/content-graph.mjs gains a drift check, assets/reel-subjects.json byte-identical to what derive_reel_subjects.py produces from the shelf right now, the same shape as the person-words.json and entity-graph.json checks beside it. Proved the deploy case itself, not only asserted it: tests/insights.mjs copies only api/ and assets/reel-subjects.json into a scratch directory with no lights/, scripts/ or tools/ at all, and runs subjectOf from a child process inside that copy, resolving a Light, a word, a verse (with its surah name) and a film correctly. Also, a refuter's review caught that subjectTop (the first ten by reach) and subjectBottom (the last five) overlap whenever fewer than fifteen posts carry a subject, so a "Weakest" list could just repeat the "Best" one; subjectBottom is now left empty below that floor, and admin2.html leaves the whole "Weakest by subject" fold out of the page rather than draw it empty. aggregate() otherwise unchanged from the design: bySubject (MIN_BUCKET 5, the same floor byKind and byHour keep), subjectTop, subjectBottom, and one plain sentence naming the best and weakest subject when two qualify. tests/insights.mjs's subject-fold section rewritten to read assets/reel-subjects.json directly with real reel ids, rather than import api/page.js with bare stub cards; 146 checks pass total.
2026-09-24 | social | edited | api/beacon.js, api/visitors.js, api/_flow.js, admin2.html, tests/traffic.mjs, tests/console2.mjs | Masterplan step 8 part three: arrivals, corrected. threads.net added to the beacon's referrer families (api/beacon.js's SOURCES), so l.threads.net and threads.net itself fold to "threads" the way l.instagram.com, lm.facebook.com and m.facebook.com already fold to "instagram" and "facebook"; a refuter's review then caught that Threads moved its own public hostname to threads.com in 2025, so threads.com, l.threads.com and www.threads.com are now matched too, threads.net kept beside them since old links and old share sheets still use it. The referrer source was counted only by month (nm:<month>:s:<src>, for "what brought them" over the season); it is now also counted per day, once per person per day the same as the monthly count, into nvh:<day>:src. api/visitors.js reads that new hash and returns `arrivals`, a row per network (instagram, facebook, youtube, threads, pinterest) with the delta, "new" when the earlier window carried nothing rather than a minus sign that reads as a drop. A refuter's review caught that the first version compared a partial today, however few hours old, plus six full days, against seven full days, which always made the newer side look weaker than it was, and called the result "this week" though it was not one; today is left out of both windows now, so thisWeek is the last seven COMPLETE days (ending yesterday, UTC) and lastWeek is the seven days before them, seven whole days each. admin2.html's own label now reads "last 7 days vs the 7 before", never "this week", so it cannot be misread as including today; the row text reads "N visits, last 7 days" against "N the 7 before". No new personal data: the same coarse source category legal.html already promises ("the day, the country, the room, and a coarse source"), counted at a finer grain. api/_flow.js's CLICK_NETS gains "threads" too, for consistency with the beacon's own family list. tests/traffic.mjs adds threads.com, l.threads.com and www.threads.com coverage beside the existing threads.net section; 70 checks pass. tests/console2.mjs's own VIS fixture and assertions updated to the corrected label and the seven-complete-day dates; 682 checks pass.
2026-09-24 | console | edited | admin2.html, tests/console2.mjs | The three blocks above, drawn in the Readers room. insHtml gains "By subject" (the same bar style as "By kind") and two lists, "Best by subject" and "Weakest by subject", each hook with its subject and its reach, reusing .fold/.bars/.list/.row and no new colour. numHtml's network list gains Threads, a fourth row beside Instagram, Facebook and YouTube, "not connected" like any other when its token is missing. A new arrivalsHtml (visitors' own `arrivals`, not the insights snapshot) draws under "The numbers": a fold, "visits this week · last", the delta in --good or --bad the same as every other delta chip in this room; a network with nothing counted either week is left off rather than shown as a flat zero. Fixed a latent bug in tests/console2.mjs found while extending it: nothing had ever stubbed GET /api/insights?action=numbers distinctly from the plain read, so numHtml was fed the wrong shape (no thisWeek/lastWeek on its rows) and threw the moment the Readers room tried to draw it, silently leaving the loading skeleton in place; added a NUM fixture in the numbers() shape and routed the action to it. Extended INS with bySubject/subjectTop/subjectBottom and VIS with arrivals, and added assertions for all of the above at both widths (390 and 1280); 682 checks pass (0 failures, versus a suite that could not previously reach a passing run once these fields existed to test).
2026-09-24 | content | edited | scripts/graph/build_graph.py, scripts/graph/derive_entity_graph.py, scripts/graph/same-as.json, assets/entity-graph.json, tests/content-graph.mjs, ARCHITECTURE.md, OPERATIONS.md, CONTENT_STATUS.md | A refuter's review found four real problems in the Content Graph batches below; fixed before this goes to review again. (1) Shelf order: besideEntity() (api/page.js) shows only the first 8 of a "lights" or "words" shelf, and derive_entity_graph.py had sorted both alphabetically, silently hiding relevant links behind an accident of spelling. by_relevance() now ranks every candidate by the mentions edge's own confidence descending, then how many distinct edges the graph records between the pair descending, then the order build_graph.py met the edge walking the source files, and alphabetical only last; an earlier attempt at "how strongly tied" (counting how many times a name repeats in one source text) was tried and dropped, since it let repetition in one word's own definition (word:bilal mentioning Makkah twice) outrank a place:p-makkah word clearly more central, such as haram or qiblah, which the distinct-edge-count version does not do. Rendered all 156 prophet, companion, character and place rooms against the fixed file and the one it replaces (report and counts in the entity-graph line below). (2) Drift guard: tests/content-graph.mjs's entity-graph check had compared each room's fields as a sorted set, "order aside", which is exactly backward once order is the promise; it now compares byte for byte, and a new check clones a room's own list, reverses it, and confirms the same comparison catches that, proof the guard is not a rubber stamp. OPERATIONS.md's description of the guard and its check count (10 to 16) updated. (3) scripts/graph/same-as.json: word:hasan (the hadith grade, "a hadith meeting every condition of sahih except...") no longer maps to companion:c-hasan; the two titles matched but the two records do not describe the same thing. This surfaced a deeper bug: derive_entity_graph.py's own "word" and "unseen" fields read the graph's raw same_entity_as edges directly, never consulting same-as.json's curation at all, so removing the pair from same-as.json alone would not have fixed it; both fields now require the curated file's own say-so, and companion:c-hasan's shelf no longer carries "word": "hasan" (the link had come from the graph's own same_entity_as edge, a 0.9 confidence title match, via slug:hasan, never from same-as.json, which is precisely why deleting only the same-as.json line would have changed nothing). same-as.json's own _about corrected (17 chapter pairs excluded, not "8 more"); ARCHITECTURE.md corrected to "63 same_entity_as edges over 61 pairs" and documents the word/unseen curation requirement. day:arafah to word:arafah and dua:w-shahada to word:shahadah were checked against their own source text and kept: the dictionary's own "arafah" entry already covers the day as well as the place, and dua:w-shahada is literally the shahada's own two testimonies, the same thing word:shahadah defines. (4) Film tracing: dropped the 0.8 "brief note" tier in resolve_field() (build_graph.py), keeping only the eyebrow at 0.9; a note is prose, not a name line, and nothing currently traces through it, so the counts (34 traced, 18 untraced) hold exactly. Added "every", "house" and "prayer" to the stopword list regardless, so a common word can never trace a film even if a future brief happens to share one with a gift's title. tests/content-graph.mjs's stale header comment (the 52 shorts "outside that count and reported, not required") corrected to describe the allow-list as it now works. All four suites rerun clean: tests/content-graph.mjs 16 checks, tests/no-dashes.mjs, tests/api-audit.mjs 139 checks, tests/rooms.mjs 600 checks.
2026-09-24 | content | edited | scripts/graph/build_graph.py, ARCHITECTURE.md, CONTENT_STATUS.md, tests/content-graph.mjs | A third pass on the 52 heroes/Hajj "short" films, using each one's own brief (tools/films/briefs/plate-<x>.json, written for the film itself, with a note and a closing eyebrow) to trace the ones a bare room fragment could not. For a heroes.html field section (several gifts, never one record on its own), build_graph.py now reads the field's own gifts (build/contributions.json) against the brief and traces to the one gift whose title or who field shares a distinctive word with the brief's eyebrow (confidence 0.9) or, failing that, its note (0.8), keeping the matched brief text as the edge's own evidence; more than one gift sharing a name, or none, stays untraced. A brief citing a Light by id ([lights/all.json: <id>], found in one brief, short-astrolabe) now also earns that film an extra reel_of to the Light. The 7 hajj.html fragments naming no node now trace to page:hajj itself at 0.7 (via section:<fragment>), honest that the film is cut from that page's section rather than a record. Traced "short" films rose from 16 to 34 (12 heroes direct, 4 Hajj words direct, 11 field gifts via brief, 7 Hajj page sections; short-astrolabe carries both a gift edge and a Light edge); the allow-list of the genuinely untraced shrank from 36 to 18 (15 field sections with no brief on file, short-darkroom whose brief names a person shared by two gifts under Seeing, short-road and short-zero whose briefs name no one any candidate gift's title or who field also names). tests/content-graph.mjs's allow-list and reason shapes updated to match, plus two new checks (a brief-matched edge carries the matched text as evidence; the one Light citation earns its extra edge); 15 checks, all pass. ARCHITECTURE.md and CONTENT_STATUS.md's generated block updated to describe and count the new rules. Nothing in a source data file renamed.
2026-09-24 | content | edited | scripts/graph/build_graph.py, scripts/graph/write_counts.py, ARCHITECTURE.md, CONTENT_STATUS.md, assets/entity-graph.json, tests/content-graph.mjs | Two follow-ups to the Content Graph batch below, before review. (1) The 52 heroes/Hajj "short" films, the masterplan's flagship derivatives: build_graph.py now traces each one's `room` field (a page and a fragment, "heroes.html#mansa-musa") to the one graph node the fragment names, where that is unambiguous, with a reel_of edge and a confidence (0.9 for a heroes.html fragment naming exactly one hero:<id>; 0.8 for a fragment on another hub page, such as hajj.html, that coincides with exactly one node of any type, since hajj.html genuinely reuses the dictionary's own "ihram" and "umrah" word ids for its own sections). 16 of the 52 now trace this way; the other 36 are flagged on their own node (`short_untraced:<why>`) rather than guessed from the reel's own title: 29 name one of heroes.html's eight field sections, which always hold several gifts and are never one record; 7 name a Hajj page section (#heart, #days, #why, #ranks, #ways) that is not a dictionary word or any other record. tests/content-graph.mjs now asserts every "short" film is either traced or named in an explicit allow-list with a reason whose shape still matches its own flag, so a film that starts tracing, stops tracing, or gets a new reason is caught rather than passed over quietly. (2) `generated`, read from a content file's own mtime, is stable only within one checkout: a runner's clone and a person's own machine set mtimes at checkout time, not the commit's, so the same tree can show two different dates on two machines. Dropped `generated` from CONTENT_STATUS.md's own generated block (the committed file); assets/entity-graph.json's `_about` never carried one. The uncommitted build/graph/noor-content-graph.json keeps its own, for whoever built it on their own machine. Confirmed by touching every file in the tree and rerunning: the same 13 checks pass, and assets/entity-graph.json and CONTENT_STATUS.md rebuild byte-identical. Every Python import across scripts/graph/ is the standard library (argparse, csv, glob, hashlib, html, json, os, re, sys, unicodedata, collections, datetime); tests/content-graph.mjs imports only Node's own fs, path, crypto, child_process and url: both run on a plain node 22 and system python3, no package install.
2026-09-24 | content | added | scripts/graph/extract_js.mjs, scripts/graph/build_graph.py, scripts/graph/validate_graph.py, scripts/graph/derive_entity_graph.py, scripts/graph/same-as.json, scripts/graph/write_counts.py, scripts/graph/run.sh, tests/content-graph.mjs, .gitignore | Masterplan step 5, the NOOR Content Graph, ported into the repository from the content audit's /root/audit/content/scripts/ (15 September 2026) so it can be rebuilt on demand instead of only outside the tree; reads the repository it lives in by default, writes to build/graph/ (git ignored here and already outside the deployment through .vercelignore's /scripts/ and /build/), and runs without the audit's optional --rooms input. The two nondeterminism sources found while porting (a page's outgoing links read from a Python set, whose order depends on the process's hash seed; one glob left unsorted) are fixed, so two builds over an unchanged tree write byte-identical files; the `generated` date is the newest content file the run actually opened, never the clock. build_graph.py now also gives every prophet, companion, character (a figure that is not the Unseen page's own copy), place and Name a real room url (/prophet/:id and so on, batch 5's rooms, which postdate the audit's own copy of this script and so were never in it) and stamps a `canonical` id on every node from scripts/graph/same-as.json, the hand kept list of the 63 records the audit found are one thing under two ids (content-003) that are a genuine duplicate rather than a chapter retelling a story under a matching name. derive_entity_graph.py reproduces assets/entity-graph.json from the graph (proved against the file as it stood before this batch: the same 169 rooms, every field the same set, zero rule mismatches); scripts/graph/run.sh builds, validates and derives in one command, then write_counts.py rewrites the generated block in CONTENT_STATUS.md. tests/content-graph.mjs is the guard: the build succeeds, validate_graph.py passes, every rendered room's node has a url, all 1,565 planned reel cards trace to a content object by a reel_of edge, assets/entity-graph.json matches the graph's own derivation, two builds agree; 10 checks, about 11 seconds. Nothing in a source data file is renamed.
2026-09-24 | content | edited | assets/entity-graph.json, CONTENT_STATUS.md, ARCHITECTURE.md, OPERATIONS.md | Ran scripts/graph/run.sh for real: assets/entity-graph.json is now generated rather than hand kept (16 September 2026). A refuter's review (below) found the first pass had sorted the "lights" and "words" shelves alphabetically instead of by relevance, which api/page.js's besideEntity() only shows the first 8 of, so the visible links on 156 rooms were partly an accident of spelling; that is fixed above, in this file (scripts/graph/derive_entity_graph.py's by_relevance()), and this is the shelf the fixed derivation actually renders. Rendering all 156 prophet, companion, character and place rooms against this file and against the hand built one they replace: 140 of 156 render byte-identical; the "words" shelf changed on 1 room, companion:c-umar (gained muhasabah, a 1.0 confidence match on Umar's full name in the word's own definition, outranking miqat's 0.8 match on "Umar" alone; lost miqat, pushed past the visible eighth); the "lights" shelf changed on 3 rooms, place:p-egypt, place:p-madinah and place:p-makkah (for example place:p-makkah gained hijri-calendar-epoch-622 and lost bilal-footsteps-in-paradise), all three where more than 8 Lights tie a place at the same confidence and the old file's order was a plain alphabetical accident, never a hand curation, so a different principled order is a change, not a regression. CONTENT_STATUS.md gained a generated counts-by-type block between marker comments (content-010: the file quoted counts the source files no longer carried); no other line of it was touched. ARCHITECTURE.md documents the content_id scheme (18 types, <type>:<id>, the Name room's own /name/<position> against the graph's /name/<slug>, a reel's reel_of edge) and the graph's new home; OPERATIONS.md names scripts/graph/run.sh and tests/content-graph.mjs among the build system and the guards.
2026-09-24 | site | edited | allah.html, muhammad.html, prophets.html, tests/rooms.mjs, tests/e2e.mjs | Audit seo-013: Arabic on these three old rooms was marked only by class (.ar, .ayah, .arn, .amiri, .font-amiri), so a screen reader or a crawler read it as English; the shell and the word pages already mark it lang="ar" dir="rtl". Every such span, static and the ones built at runtime, now carries both attributes, no class or style touched, no look changed; three of allah.html's own createElement calls (the notable-verses list, the al-Hashr verses, its fourteen name chips) had no literal class="..." in the source at all to catch by a source scan, and shipped unmarked on a first pass. tests/rooms.mjs keeps a source-level scan of the static spans and of allah.html's three createElement lines; tests/e2e.mjs section 12 is the live check, walking each page in a browser and asking every Arabic-marking element whether it or an ancestor actually carries lang="ar".
2026-09-24 | site | edited | api/page.js, tests/rooms.mjs | Audit seo-026: /today and /today?date=<d> pointed their own canonical at themselves instead of the light room the date resolves to; todayPage() now sets canonical to /light/<id> for any resolved day that is not today itself, leaving /today's own canonical alone. tests/rooms.mjs checks the exact canonical tag for a dated request against /light/battle-of-badr-624, separately from og:url which still carries the date.
2026-09-24 | site | edited | api/page.js, tests/rooms.mjs | Audit perf-018: the verse room's reel used preload="metadata"; the home page already waits for a tap. Verse room video now reads preload="none" to match. tests/rooms.mjs checks the verse room's video tag.
2026-09-24 | site | edited | protection.html, marriage.html, soul.html, teens.html, sermon.html, good-life.html, hajj.html, pillars.html, simulation.html, three-lives.html, masjid/start.html, api/page.js, tests/rooms.mjs | Audit seo-019: ten static Article pages carried no author and no image in their JSON-LD, and masjid/start.html's HowTo carried no step though its own headings already give the steps in order. Each Article now names the house as an Organization author with the site's own og image; masjid/start.html's HowTo now carries the same thirteen steps as its own h2 headings, nothing invented. The surah room's Chapter ld gains its publisher. A Light and a chapter's Article print no datePublished or dateModified: a last-edit date is not a publication date and no truthful per-item date exists yet, so the house rule (every date printed is true) leaves the field out rather than guess. tests/rooms.mjs checks all of the above by parsing each page's own rendered or static JSON-LD.
2026-09-24 | site | edited | theology.html, kids/mushaf.html, kids/letters.html, kids/cradle.html, tests/rooms.mjs | Audit seo-018: theology.html's own #women links (from index.html and ask.html) had no landing id; its "Men and women in the law" section now carries id="women". Three kids pages linked their wordmark at /index, a path the router does not serve; changed to /. tests/rooms.mjs checks both.
2026-09-24 | site | reverted | index.html, ar/fr/es/de/ru/tr/ur/hi/bn/id/fa/prs/pa/index.html (13 doors), ps/ha/ja/ko/ku/so/sw/zh/index.html (8 thin doors), sitemap.xml, tests/rooms.mjs, tests/home2.mjs | Audit seo-010, reverted whole: NOOR.md itself says per-language SEO is paused by Sam, and the finding's own recommendation was to act only when the owner wants it moved. Every hreflang line on the home page, all 21 door files and every sitemap xhtml:link block restored to exactly what /root/noor carries, prs included as the tag rather than fa-AF; tests/rooms.mjs's whole seo-010 block and tests/home2.mjs's fa-AF change reverted with it. Diffed file by file against /root/noor to confirm no hreflang line differs. The sitemap's other two seo-014 fixes (no trailing slash on /masjid or /stories, /three-lives listed) are the only sitemap.xml changes that remain.
2026-09-24 | site | edited | sitemap.xml, tests/rooms.mjs | Audit arch-022: /three-lives was a whole indexable room the sitemap never named. Added, with the same content date every other static entry carries.
2026-09-24 | site | edited | sitemap.xml, api/sitemap.js, vercel.json, masjid/index.html, tests/rooms.mjs | Audit seo-014: /masjid/ and /stories/ in sitemap.xml redirect to /masjid and /stories; fixed, and masjid/index.html's own canonical corrected to match. Verse room lastmod now reads a reel's own uploaded date, from reels/verse-<id>.json where one exists, before falling back to the content date constant; vercel.json's includeFiles for api/sitemap.js widened to ship those sidecar files. No date anywhere is invented. tests/rooms.mjs checks the sitemap carries no trailing-slash entry and that the sidecar files ship.
2026-09-24 | site | edited | kids/strong.html, api/page.js, tests/rooms.mjs | The refuter's review of seo-007's new description and of chapterDesc: kids/strong.html's game is five days, not three ("In five days he turns SIX," Day 1 through Day 5 on the page itself), "a three day story game" corrected to "a five day story game." api/page.js's chapterDesc had two of its own bugs: chapter 8 (Qur'an 11:44) could stop mid-quotation, since the sentence split runs on every period and a quotation three sentences long has two of them inside it; it now folds a quote back together with however many of the following fragments it takes to close before it will use any of them, and simply never selects a quote that never closes within budget. Chapter 69 repeated its own summary ("Books fly.") as "The books fly.", the details' very next sentence; a candidate sentence is now dropped if it or the text gathered so far contains the other, once both are folded to bare words. A third fix found in the same review: the first version gave up at the first sentence too long to fit and left five chapters (19, 22, 23, 57, 62) under 50 characters; it now skips a too-long or already-used sentence and keeps looking, so all 71 chapter descriptions honestly clear 50 characters from their own text, none padded, none invented. tests/rooms.mjs now renders and checks all 71: length, no open quotation, and every description word traced back to the chapter's own summary or details.
2026-09-24 | site | edited | 33 pages (protection.html, hajj-plan.html, soul.html, eid.html, masjid/index.html, madrasa.html, stories/shakur.html, hajj.html, masjid/khutba.html, unseen.html, begin.html, stories/jabbar.html, donate.html, heroes.html, sermon.html, masjid/timetable.html, stories/sabur.html, stories/razzaq.html, masjid/qibla.html, teens.html, stories/hadi.html, masjid/start.html, three-lives.html, school.html, ramadan.html, good-life.html, kids/cradle.html, mizan.html, journal-rules.html, simulation.html, arabic.html, theology.html, kids/letters.html), kids/ark-pairs.html, kids/echo.html, kids/star-catcher.html, kids/island.html, kids/strong.html, api/page.js | Audit seo-007: descriptions over about 160 characters trimmed at a sentence or word boundary, nothing added; five kids games that carried no description meta at all (ark-pairs, echo, star-catcher, island, strong) each get one plain factual line. The Path's chapter rooms, whose descriptions ran under 50 characters, now build a real one from the chapter's own summary and detail sentences (api/page.js's chapterDesc), joined only until long enough, never invented.
2026-09-24 | site | edited | api/page.js, journal.html, three-lives.html, tests/rooms.mjs | Audit seo-006: rendered rooms' title suffix shortened from " · NOOR Codex of Light" to " · NOOR" (og:site_name stays the full name); api/page.js's shell() no longer trims the sentence in front of it at all, since the audit itself found the sentence titles the site's best hooks and an earlier pass here had cut one ("...is probably legend" into "...is", asserting the opposite). Of nine static pages that first pass shortened, seven had only the suffix changed and stayed as they were; journal.html and three-lives.html had lost words from the title itself ("thoughts from inside the work" and "the full ledger, the worst life, and the balanced one") and are restored complete, suffix only shortened. tests/rooms.mjs checks the new suffix.
2026-09-24 | tests | edited | tests/rooms.mjs, tests/home2.mjs, tests/e2e.mjs | Audit arch-013: tests/rooms.mjs's manifest-stub issue was already fixed (a symlink farm excluding reels/, 16 September); ran clean, 356 passed, 0 failed. tests/home2.mjs's "without the manifest" block now refuses /reels/index.json and /reels/home.json outright instead of relying on a static server's lack of an /api, which no longer proves anything since reels/index.json is a real file any server serves; while proving that fix, found and fixed a second, larger stale assumption in the same block: the walk that decides which half of the day carries today's verse read ROTA[half][day] straight, missing the afternoon to afternoonUntilShorts swap the real site makes whenever the shelf carries no shorts (it does not, in the test's own fixture), so on a day the two rows disagree, today among them, the test chased the wrong card through five downstream assertions; it now calls the page's own rotaRow the way verse() itself does, and all five pass. tests/e2e.mjs's two ".n2-idea" counts of 6 raised to 7, matching the arrival's own seven screens. tests/social-footer.mjs's "62" page count already matches the tree (arch-013's own "63" was itself stale); tests/mushaf-engine.mjs's mirror-count assertion was already corrected, dated in its own comment; neither file touched. Three tests/home2.mjs line-count failures (fonts.googleapis.com blocked from this box, so the fallback font wraps differently) and, found but not fixed since neither is named in arch-013's own evidence and both are present on the base commit before this batch: 5 more tests/e2e.mjs failures (a first-screen door count, a search count, mizan's sheet, admin's journal count, the sponsor page's give link) and 2 tests/social-footer.mjs failures (the front page row's order, the label text) that read as a UI change the tests never caught up to; reported for the Director rather than guessed at or weakened.
2026-09-24 | docs | edited | OPERATIONS.md | Audit arch-019: section 5 named 30 routes and missed /api/page, /api/sitemap, /api/threads and /api/youtube, still listed the removed /api/license as live, and called /api/daily-light a caller of illuminations after this same batch removed it (see the arch-020 line below); all four corrected, and the two remaining 410 stubs (guardians, sponsor-checkout) described as what they are. Section 2 gains three tables (Pinterest, Threads, YouTube) and one paragraph naming the tuning variables the poster reads but the file never listed, CHANNEL_MIN_MS through YT_CLIENT_SECRET, plus a line each for VERCEL_URL (Vercel's own, never set by hand) and why LEDGER_FLOOR stays out, per NOOR.md. Section 0's branch corrected from noor-v2-illuminated to main, matching every workflow, and its route count from 30 to 32.
2026-09-24 | site | edited | hajj.html | Audit perf-008: the two badges beside a long chapter heading ran past the right edge at phone widths; under 430px .ch now wraps and .chb sits full width, left aligned, so nothing is clipped. The Mushaf reciter select in quran.html is the other half of this finding; quran.html belongs to another batch and was left untouched.
2026-09-24 | site | edited | assets/noor2.css, index.html | Audit perf-020: the surah heading links on the verses shelf (.n2-list-a) sat at 21px tall; now display:block with a 44px minimum height and 10px of padding. The footer's language links (.hm-langs) get line-height:2.2, so a wrapped row keeps clear space between the lines. Neither had a test depending on the old measurements.
2026-09-24 | kids | edited | kids/ark-pairs.html, kids/cradle.html, kids/echo.html, kids/island.html, kids/kaaba-builder.html, kids/lanterns.html, kids/letters.html, kids/orchard.html, kids/practice.html, kids/star-catcher.html, kids/story-steps.html, kids/strong.html, kids/yunus.html, kids/zamzam.html, kids.html | Audit perf-017: defer on kids-engine.js was tried and reverted after Playwright proved it breaks every game silently: each game's own inline script runs synchronously during parse, before a deferred script executes, and its `var NK = window.NK || (a no-op shim)` line binds permanently to the shim, so sound, haptics and sparkle go quiet with no page error at all. kids-engine.js stays plain. Instead dropped noor-dials.js, the one kit file confirmed unused on every one of the fifteen games and the kids.html hub (none carries #site-header for its notice banner or a /journal link for it to hide), removing its unconditional /api/settings call from fifteen pages. Verified: every game and the hub load with no page error (tests/e2e.mjs's kids section, 30 checks, all pass).
2026-09-24 | site | edited | noor-fx.js | Audit perf-015: the "Follow the light" label under the social row read 10px in dim gold, 3.11:1 on the night; raised to 12px in rgba(255,254,247,.62), the shell's own parch3, clearing 4.5:1.
2026-09-24 | infra | edited | vercel.json, tests/rooms.mjs | Audit perf-014: the individual reel sidecars under reels/*.json, beside the already-cached index.json and home.json, carried no cache lifetime at all. The existing /reels/(index|home).json rule widened to /reels/(.*)\.json, the same policy index.json and home.json already proved safe, so every reel sidecar is kept five minutes and revalidated in the background. noor-fx.js and the other unversioned root scripts are left as they were: Vercel already purges its CDN on every deploy, and a browser-side stale-while-revalidate of a day on a script with no ?v= tail is a real mismatch risk an edge-only rule does not fix, so no new header rule was added for them. tests/rooms.mjs's reels-cache line updated to the widened pattern.
2026-09-24 | animation | verified | allah.html | Audit perf-011: already fixed on 16 September (see that day's line below); the field of light draws once and stops under prefers-reduced-motion, and the loop already stands down on visibilitychange. The canvas is position:fixed over the whole viewport, so it is never scrolled off screen; nothing further changed.
2026-09-24 | api | removed | api/daily-light.js, tests/api-audit.mjs | Audit arch-020: api/daily-light.js had no caller anywhere in the tree (checked vercel.json's crons and rewrites, every html and js file, the tests and the workflows); removed the dead route and its now-meaningless entry in tests/api-audit.mjs's writer-guard list. The markets pane on admin2.html was left alone: admin.html, which the audit's recommendation waited on being retired, is the full old console and is never changed under the owner (NOOR.md), so its condition for dropping markets.js does not hold; reported rather than acted on.
2026-09-24 | site | edited | ask.html, mizan.html | Audit arch-003: both still carried the shell's old cache-buster, v=5, while api/page.js, noor-fx.js and every other old room had already moved to v=14; brought both to v=14 so a fix to noor2.css or noor2.js reaches them with the rest of the house. No other page was found stale.

2026-09-17 | content | edited | api/page.js, tools/reels/verses.py, tools/reels/verses.json, tests/rooms.mjs, tests/home2.mjs | Audit content-009: 63 of 109 surahs with shelf verses were named two ways, the live Qur'an API's englishName and tools/reels/quran-uthmani.json's own translit. api/page.js's verse and surah rooms now print the uthmani table's name always, never the API's; tools/reels/verses.py's fetcher now stores that same name in verses.json instead of the API's, and the 572 verse rows it had already stored corrected to match, no network call made. tests/rooms.mjs gains a check that five surah rooms and all 591 already-rendered reel captions carry the uthmani name, and a prior assertion that had been proving the API's escaped name correct now expects the uthmani one.
2026-09-17 | content | edited | scripts/build.mjs, scripts/patches/seerah.mjs | Audit content-008: three of the Path's chapters, Uhud, Hudaybiyyah and Fath Makkah, repeated one fact under two labels once a patch's addFacts merged with the base chapter's own. scripts/build.mjs's fact merge now also checks a normalised numeric value against every fact already on the node, not the label alone, before it merges; scripts/patches/seerah.mjs's three duplicate rows removed at the source. node/39.json, node/41.json and node/42.json regenerated; the other 68 chapters came back byte for byte the same on a second run.
2026-09-17 | content | edited | scripts/gen-stories.py, build/stories-a.json, build/stories-b.json, stories/sabur.html, stories/adl.html, stories/shakur.html, stories/jabbar.html, stories/index.html | Audit content-006: four of eight door stories, As-Sabur, Al-Adl, Ash-Shakur and Al-Jabbar, gave their Name a meaning that disagreed with allah.html's own NAMES row. scripts/gen-stories.py now reads the meaning straight from that row by transliteration at build time and stops if a story's Name is not one of the 99; name_meaning dropped from build/stories-a.json and build/stories-b.json, no longer typed twice. The four story pages and the doors on stories/index.html carry the corrected meaning; nothing else on any page changed.
2026-09-17 | content | edited | allah.html, index.html, muhammad.html, mizan.html, tests/no-dashes.mjs, tests/home2.mjs | Audit content-005: 185 em dashes in reader-facing text replaced with a comma or a colon, whichever reads well in that place, never a plain deletion, 179 of them in allah.html's NAMES script, one in index.html's meaning of 2:186, five in muhammad.html and mizan.html's descriptions. tests/no-dashes.mjs now also scans allah.html, index.html, muhammad.html and mizan.html line by line, on top of the film scripts it already covered; tests/home2.mjs's own copy of the 2:186 meaning updated to match.
2026-09-17 | content | edited | allah.html, assets/search-index.json, tools/reels/plan.json, tests/rooms.mjs | Audit content-004: two Names shared the transliteration Al-Majid, rows 48 and 65; row 48 now reads Al-Majeed and row 65 Al-Maajid, the split the reel and dictionary collections already used, Arabic unchanged. Every row of allah.html's NAMES array now carries its own slug id, so no reader has to derive one from the transliteration again. tools/reels/plan.json's two name cards and tests/rooms.mjs's room-by-number check updated to the new spellings; assets/search-index.json regenerated; python3 copy_audit.py plan.json still reads 1565 cards, 0 findings.

2026-09-17 | films | edited | tools/films/TEMPLATE.md, tools/films/shortplate.py, tools/films/briefs/plate-*.json (22), tools/films/films/short-*.json (22) | The writing law and the sound law added to the template on the owner's verdict of the rendered films (cryptic words, loud bells): every film's words rewritten so a stranger who scrolls fast follows them with no picture (a plain hook, the person with his role, one complete step a line, the technical word with its plain word beside it, the payoff in the viewer's world), fact checked line by line by the refuter against the pages they come from, with four facts corrected from the page; the reading model's ceiling raised from 46 to 52 s so a sub line can carry the context where it is needed; the dark room's two lines brought to the law.
2026-09-17 | site | edited | scripts/oldrooms.py, the same 51 old rooms, tests/rooms.mjs | The refuter's review of the batch 6 diet: with no script, html,body{color:#FFFEF7!important} never reaches an element carrying a color rule of its own, and nearly every heading, paragraph and card in these rooms does, so a reader with JavaScript off or blocked got the night's own dark background with the room's own dark ink still sitting on it, one contrast of 1.23:1, a few of those same cards keeping their own white background too, and no way off the page at all, the header stub having no links. scripts/oldrooms.py now reads assets/noor2-night.css itself for the answer rather than guess a room's classes a second time: that generator already worked out, room by room behind a data-room gate noor-fx.js sets, which of a room's own rules (an ink heading or paragraph, a card's own white background, a couple of gradients) flip for the night, and every one gated to a page's own room is now written onto that page directly, gate stripped, !important; the two pages the generator never saw, allah.html and muhammad.html, are answered instead straight off their own ink and white-card rules, the same shape in miniature. 1,368 such rules answered across the 51 rooms, none left over. The header stub's noscript block, rendered only when there is no script to run it, now also carries a five door menu, Home, Today, The Lights, The Verses and Search, and a <style> of its own that turns the hidden header back on for it; noor-fx.js's own bar and its own #site-header{display:none!important} are unaffected, since a browser only ever builds a noscript element's children when scripting is off, so there is nothing here for a script to fight. Reverified with the same Playwright check the refuter ran (javaScriptEnabled:false against the local server): every genuine ink text node and every card on stories/adl.html now clears 18.6:1, not 1.23:1 or white on white, and the five links are present and reachable; with JavaScript on, a sweep of all 51 rooms still shows the bar present, search opening and no page error, unchanged. tests/rooms.mjs re-derives the same read of assets/noor2-night.css independently and checks every override it names lands on its page, and that every stub's noscript carries the five doors.

2026-09-17 | films | edited | .github/workflows/films.yml, .github/workflows/tests.yml, tools/films/render_ci.py | The refuter's review of the films workflow: the briefs, machines and proof inputs were interpolated straight into shell with ${{ }} in run steps, a script injection path under contents:write and pull-requests:write; all three now travel through env: and are read as shell variables. machines is validated a whole number from 1 to 8 with a clear line rather than a Python ValueError three steps downstream. The comment describing the shelf upload wrongly said REELS_STORE=blob was set; it now says blob_put is called directly and never reads REELS_STORE at all. download-artifact now tolerates a batch where every machine made nothing (continue-on-error, the next step checks the directory exists) rather than failing assemble outright. render_ci.py's score_and_mux renames a film back from shortmusic.py's hardcoded 60fps name in a finally, so a killed job or a raised exception never leaves a film stuck under that name. tests.yml's SUITES list now runs films-workflow. Reverified: node tests/films-workflow.mjs (33 passed), py_compile, a dash scan, and reels, reels-kinds, retire, shorts, no-dashes all still green.

2026-09-17 | social | edited | api/social.js, tests/shorts.mjs | The refuter's review of the dual YouTube upload: a wide upload left {pending:id} beside a Short that already went out was never settled, since finishPendingReels skips any result with ok true and FINISHERS had no youtube key. A new finishYouTubeWide, added to FINISHERS, and a new check mirroring the existing story.pending pattern (visited even when the top result's ok is true) now read results.youtube.wide.pending through YT.status into wide:{id,url} or wide:{refused}, one look, never a fresh upload.
2026-09-17 | social | edited | api/social.js, tests/shorts.mjs | sendYouTubeBoth dropped the wide upload with no record at all when the day's cap was already full by the time the Short's own upload had counted against it; it now records wide:{skipped:"cap"} so the console shows why, and never retries it later, since the owner's own rule is that a Short always wins the room.
2026-09-17 | social | edited | api/_channels.js, tests/shorts.mjs | A short row with a wide file but no story fell through shapeRaw's whole short branch, guarded on p.story as well as kind, to the generic path, which gave the Short the film's plain title plus #Shorts and made shapeYouTubeWide return null. The branch now fires on kind "short" alone, falling back to the row's own caption wherever it used to read story, so a story-less short still gets the hook title and its wide upload.
2026-09-17 | insights | edited | api/_insights.js, api/warm.js | snapshot()'s out.written++ sat outside the kvReady check and counted a write that never happened whenever the store was not ready; moved inside it. warm.js's own call now checks kvReady() first and records stats:{skipped:"store not ready"} instead of importing _insights.js and asking the network for nothing the store could keep.
2026-09-17 | insights | edited | api/_insights.js | Every Meta and YouTube fetch in this file (graphGet, used by fbMetricSet/fetchInstagram/fetchFacebook, and fetchYouTube's own call) now carries an eight second AbortController timeout, so one hung call cannot spend the whole run; snapshot() also checks its own clock right after the token exchange and before the first store round trip, so a slow token alone cannot burn the budget unnoticed.
2026-09-17 | insights | edited | api/_insights.js, tests/insights.mjs | numbers() counted a short's wide upload as a second post in every bucket grouped by kind, weekday or slot (not only byNetwork, which already remapped it): the wide's views are now folded into its Short's own "youtube" row before any bucket is built, so a slot with both counts once, and the now-redundant byNetwork remap is removed.
2026-09-17 | insights | edited | api/_insights.js | bucketDelta read a week with nothing the week before as a null delta, indistinguishable in the console from an error; when the prior bucket has no posts at all the views, reach and engagement deltas now read "new" instead.

2026-09-16 | site | edited | scripts/oldrooms.py, noor-fx.js, the same 51 old rooms, tests/rooms.mjs | Audit arch-005: noor-search.js comes off every old room, since noor-fx.js already fetches it itself on first keystroke or a tap on the bar's More door (NOOR_NEED_SEARCH). noor-ramadan.css, noor-hijri.js and noor-ramadan.js come off every old room except ramadan.html and eid.html, whose own inline scripts read window.NOOR_HIJRI and window.NOOR_RAMADAN directly for permanent calendar content and so need the season loaded unconditionally; noor-fx.js gains a small date gate, Ramadan and Dhul Hijjah start dates worked out through 2034 from the same tabular calendar noor-hijri.js uses, that now loads those three files itself, only in the weeks they actually fall due or when ?season= asks for it, guarded against loading twice. Nine other kit files, noor-ink.js, sponsor.js, noor-anime.js, noor-overrides.js, noor-text.js, noor-dials.js, noor-rtl.css, figfit.css and figfit.js, stay tags on every page that already carried them: noor-fx.js has no on-demand path for any of them, and each does something the room needs at once. tests/rooms.mjs checks the season kit is off 49 of 51 rooms and kept on the two calendar pages, and that noor-fx.js carries the date gate.
2026-09-16 | site | edited | scripts/oldrooms.py, the same 51 old rooms, scripts/fouc.mjs, scripts/measure.mjs, tests/rooms.mjs | Audit perf-003: every old room's head now links the shell's four stylesheets, noor2.css, noor2-skin.css, noor2-night.css and noor2-legible.css, as ordinary tags, and carries a 104 byte inline style, html,body{background:#0A1024!important;color:#FFFEF7!important}#site-header{display:none}, so the page paints the shell's own night colour from the first frame instead of the parchment body style painting first and then being swapped out once noor-fx.js runs. The !important is safe only because it repeats the exact colour noor2-night.css itself sets on html.n2-night body once that class lands; it never disagrees with the theme, only stands in ahead of it. fouc.mjs, run against five old rooms on mobile and desktop, shows the parchment flash and the old header both gone from the very first sample, where before some pages showed it for up to 698 ms. measure.mjs on quran.html shows fewer requests and less weight shipped, 21 against 25 requests, 162 against 188 KB gzipped, but is one run and moved the wrong way on LCP and CLS in that same run, so fouc.mjs is the steadier evidence for this finding. tests/rooms.mjs checks the style and all four links on every old room.
2026-09-16 | site | edited | scripts/oldrooms.py, the 51 old rooms it lists (36 root pages, six masjid pages, the nine stories), tests/rooms.mjs | Audit arch-004: every old room's baked header, roughly 21 KB of Tailwind markup noor-fx.js only ever hid and then built its own bar beside, is now a hidden stub of three elements, #site-header, #search-toggle and #hm-search, the only ids any script in the house still reaches for. Two pages, allah.html and muhammad.html, carry a sixth header shape with noor-fx.js's own script tag baked inside the header block rather than in head, and their whole kit of five tags at the end of body rather than in head as every other room carries it; scripts/oldrooms.py hoists any embedded script or stylesheet tag out in front of the stub before replacing the block, so the shell loader is never deleted along with it, and the same three kit tags come off their end of body as come off every other room's head (see the arch-005 line below). A Playwright sweep of all 51 rooms shows zero page errors and the shell bar present after noor-fx.js runs, both before and after this change. tests/rooms.mjs gains a section checking the stub's three ids and its under 500 byte weight on every room, and that scripts/oldrooms.py is idempotent.

2026-09-16 | social | added | api/_youtube.js, api/_insights.js, api/insights.js, api/warm.js, vercel.json, admin2.html, tests/insights.mjs, SOCIAL_ENGINE.md | Masterplan step 8: the house now reads its own numbers per post, per network, per kind and per weekday, every day, rather than only on demand. YT.ytStats (api/_youtube.js) reads videos.list part=statistics,contentDetails for up to fifty ids a call, errors folded to {error} and never thrown. A new snapshot() (api/_insights.js) walks the slot records of the last fourteen days (bounded to thirty, and by a forty second clock), asks Instagram, Facebook and YouTube for the numbers on every live id a record carries, the Short and its wide upload both, and writes one row per network to its own key, nsoc:stats:<date>#<slot>, thirty kept, idempotent per day unless forced; kept apart from the poster's own nsoc:slot key so the two writers never race. A new numbers() folds a fortnight of those photographs into this week against the one before it: per network (posts, views, reach, engagement rate, summed over the bucket), per reel kind and the silent films together (card kinds excluded, since the rota does not choose a card the way it chooses a reel), per weekday, per slot hour, the best and worst kind by engagement, and every film named once any exist; reads the snapshots alone, no network call. api/insights.js gains POST {action:"snapshot"} (owner gated, or the same CRON_SECRET bearer api/social.js's `due` action already accepts) and GET ?action=numbers (owner only). The daily photograph rides api/warm.js's existing 04:00 UTC run rather than a fourth vercel.json cron, since the project already carries two and tests/rooms.mjs holds it to exactly that count; api/warm.js's own maxDuration raised to 120 in vercel.json to give the snapshot (budgeted to 25 s there) room beside the night shift. admin2.html's Readers room gains a phone first card, "The numbers": this week against last per network, the best and worst reel kind, the films' own rows once any exist, a Refresh button that snapshots then re-reads, and "not connected" rather than a silent zero for a network with no token. tests/insights.mjs extended: ytStats batching and error paths, the snapshot writing a record with Instagram, Facebook, a Short and its wide upload, the bounded walk (a day count clamp and a spent budget stopping before a single network is asked), and the numbers() arithmetic on a fixture of eight records across two weeks, including that no token or secret name appears in either answer. SOCIAL_ENGINE.md gains a Numbers section; its "What remains" Analytics line is narrowed to what is still open, retention proper and conversion, and that nothing yet feeds the rota. 113 checks pass in tests/insights.mjs (69 prior, 44 new); tests/youtube.mjs, tests/social.mjs, tests/crongate.mjs, tests/house.mjs, tests/api-audit.mjs and the rest of the suite touching api/social.js unaffected.
2026-09-16 | tests | added | tests/films-workflow.mjs | A string and regex check of .github/workflows/films.yml itself (the suite runs on Node 22 with no YAML parser), the same way tests/retire.mjs already checks reels.yml: the three workflow_dispatch inputs (briefs, machines, proof), the plan, render and assemble jobs each with a timeout, one concurrency group so two runs never race, the films-music release name, that the render job calls render_ci.py and the assemble job calls shortmanifest.py, that publish-shorts.sh is named in the file's own prose but never actually run, that the only two secrets read anywhere are GITHUB_TOKEN and BLOB_READ_WRITE_TOKEN, and no em or en dash anywhere in the file.
2026-09-16 | films | added | .github/workflows/films.yml, tools/films/render_ci.py, tools/films/README.md, HANDOFF.md | A GitHub Actions workflow so a staged plate brief reaches the shelf with one click on the Actions tab, no Terminal: plan (compiles every named or staged brief with shortplate.py --check, fails the run with the PROBLEMS lines if one is refused, splits the rest across up to 8 machines), render (a matrix; each machine calls the new render_ci.py per film, compiling, rendering both shapes at 30 fps with a three sample shutter, or 12 fps shutter 1 for the proof input, laying the score under each with shortmusic.py --mux (the same rename dance plates.sh does around its hardcoded 60fps name), auditing each with audit.py (an audit FAIL ships its film, only a render or mux failure stops one) and drawing a ten tile contact sheet of each, one tile at each line's own midpoint, with PIL, since contact.py samples one frame a beat and a plate short's whole picture is one beat), and assemble (builds the rows with shortmanifest.py unedited, uploads each finished short to the shelf at the same reels/<slug>.mp4 pathname and under the same 50 MB ceiling publish-shorts.sh's own delivery copy keeps, using shelf_store.py's own blob_put helper with the blob path chosen rather than a GitHub Release so a film built here and a film the owner publishes by hand write the same kind of row, merges the rows into reels/index.json and reels/home.json the same deduplicating way publish-shorts.sh's own merge does, and opens one pull request on branch films/built with every audit's table in its body). The two Suno scores are never committed: tools/films/README.md, "The music release", is the owner's own four steps to put them on a release tagged films-music, which the render job downloads with the gh CLI and fails clearly if missing; "The shelf" names BLOB_READ_WRITE_TOKEN as the one repository secret the run needs (the same token tools/reels/README.md already asks for), added at the repository's own Actions secrets, never in a file. Proved locally: render_ci.py darkroom --proof ran the full chain end to end (both shapes rendered, scored, audited, ten tile contact sheets drawn); both audits FAILed on content the engine itself draws (a quiet pacing window, the wide frame's watermark corner), which is exactly what letting a FAIL through is for. HANDOFF.md's commands section now names the films workflow and its two requirements.

2026-09-16 | social | edited | api/social.js, api/_youtube.js, api/_channels.js, SOCIAL_ENGINE.md | The owner: a short with a wide file should reach YouTube twice, the tall file as a proper Short and the wide file as an ordinary 16:9 video, never one in place of the other, keeping the 16:9 for longer material. shapeRaw's youtube branch now always builds the Short from the tall file, titled the hook (not the film's title, which post.title carries for Pinterest); a new shapeYouTubeWide (api/_channels.js, calling a new YT.shapeWide/titlePlain) builds the second, ordinary video, titled the film's own name, no #Shorts. sendOne's new sendYouTubeBoth (api/social.js) sends the Short first, always counting it against YT.DAILY_CAP first, then the wide file only if the cap still has room after; a wide upload YouTube refuses outright, or a confirmed duplicate (YT.status, read the one way fbReelStatus reads Facebook's, a single look since the refusal sometimes only surfaces once processing finishes), is recorded results.youtube.wide.refused without touching the Short's own ok; one still processing at that one look is wide.pending. Both uploads live under the one "youtube" result, so findDuplicate and notePostedChannels still see one send to the channel, not two. upload()'s hardcoded /shorts/ url is now conditional on opts.short (default true, unchanged for every existing caller), so the wide video gets a plain watch url instead of a Shorts one it was never filed as.
2026-09-16 | house | edited | tools/reels/README.md, SOCIAL_ENGINE.md | The refuter's review of the shorts batch: both documents described a short's row and its per-network shaping as already reaching production, when buildSlot's missing carry (below) meant the shaping branch never actually ran and the YouTube video fix (below) had not yet landed. Both files now say plainly that shortmanifest.py writes no wide field, that publish-shorts.sh's merge is the only place a real wide url is ever written, that buildSlot has to carry a short's own fields onto the post or the shaping never runs, and that YouTube keeps whichever video the shaper chose.
2026-09-16 | films | edited | tools/films/shortmanifest.py | The refuter's review: _pick_card returned the first card of a field the moment nothing scored above its starting value, so a brief sharing no real word with any card in an ambiguous field still got one, silently, as if it matched. best_score now starts at 0, not below it, so a card is only picked once its score is strictly above zero, and _pick_card returns nothing otherwise, exactly as ties already worked. page_text prints a warning naming the brief when this happens; story_for was already built to fall back to the brief's own lines when the page text comes back empty, so the fallback needed no change, only the wrong pick that was hiding it.
2026-09-16 | films | edited | tools/films/shortmanifest.py | The refuter's review: plate_row wrote wide as a bare filename (short-<slug>-wide-30fps.mp4) the moment a wide render existed on disk, before any upload had happened; publish-shorts.sh's merge only ever overwrote that key when an upload succeeded, so a wide upload that failed or was refused for size left the bare filename standing in the published row, and every reader downstream (buildSlot, the youtube shaper, sendOne) would have treated it as a playable url. The key is no longer written here at all: it exists only once, as a real https url, once publish-shorts.sh's own merge has recorded a successful upload.
2026-09-16 | films | edited | tools/films/publish-shorts.sh | The refuter's review: the merge's by_id map was built by enumerating reels/index.json's own rows, so a shelf that already carried the same id twice (an old merge bug) could only ever have ONE of those rows replaced; the other stale copy was neither updated nor removed and kept posting whatever it still said. The existing shelf is now deduplicated by id first, keeping the position of each id's first row and the content of its last, before this run's own rows are merged in, so a repaired shelf converges to one row per id instead of carrying the fault forward.
2026-09-16 | films | edited | tools/films/publish-shorts.sh | The refuter's review: this script's own HOME=../../reels/home.json reassigned the shell's HOME, and bash keeps a variable exported once it already is, which HOME always is, so every child process below (ffmpeg, node, python3) inherited a working directory named reels/home.json in place of a real home. Renamed to HOMEJSON; nothing else in the script changed.
2026-09-16 | social | edited | api/social.js | The refuter's review: sendOne's YouTube branch handed fn({ ...shaped, video: p.video }), which always overwrote whatever video shapeRaw's short branch had chosen (the wide file when the row has one) with the tall file, so YouTube never once received a wide short. Now video: shaped.video || p.video, so the shaper's own choice stands and an ordinary reel, whose shaper sets no video, still falls back to p.video exactly as before.
2026-09-16 | site | edited | index.html | The refuter's review: the home page's own copy of ROTA.afternoon, kept letter for letter with api/_schedule.js's so the page picks the same short on the same day the poster does, was never updated when the afternoon split to four short days; it still read seven "short"s and tests/home2.mjs's own letter-for-letter check was failing. Copied the real row across: ["short", "word", "short", "verse", "short", "know", "short"].
2026-09-16 | social | edited | api/_schedule.js | The refuter's review: buildSlotInner's reel branch, which every reel slot including a short runs through, built the post from only title, oneLine, body, caption, link, image, video and kind, so a short's post never carried story, its own title, hook, payoff, tags, wide or src; api/_channels.js's whole per-network short-shaping branch reads exactly those fields and so had never actually run in production, on any network, since it shipped. Carried onto the post for kind "short" only; an ordinary reel's post is built exactly as before.
2026-09-16 | films | edited | tools/films/publish-shorts.sh, tools/reels/README.md | The rendered masters are CRF 16 and weigh 90 to 180 MB, above Telegram's 50 MB upload ceiling and heavier than any network needs. publish-shorts.sh now makes a delivery copy of each shape first with ffmpeg (libx264, preset slow, crf 21, yuv420p, faststart, audio copied) at out/deliver/<slug>-<shape>.mp4, skips the transcode when the copy is already newer than its master, refuses to upload a copy still over 50 MB (its size printed, the run moving on to the next file), and uploads the copies, never the masters.
2026-09-16 | social | edited | api/_schedule.js, SOCIAL_ENGINE.md, tools/reels/README.md | The afternoon rota asked for kind "short" on all seven days, but no row of that kind ever reached the shelf, so the slot fell through to the ledger every time; a finished plate film had no path from tools/films/ to /reels/index.json at all. The afternoon rota is now four days of "short" (Sunday, Tuesday, Thursday, Saturday) and three of the old sequence (word, verse, know), so the house keeps its voice on the other three days once shorts do start posting; the split is self disabling, a day asks for "short" whether or not any exist, and chooseReel already falls back when the shelf is empty. reelRoom had no case for kind "short" at all, so a short's outbound link would have gone to the site's front door instead of the library room its own brief points at; it now reads the row's own room field, the same way every other kind reads its own link field. Documented in both files with the shelf's pipeline and the self disabling design.
2026-09-16 | social | edited | api/_channels.js | shapeRaw had no branch for kind "short": a plate film's long story, its source line and its room link had nowhere to go on any network. Added one block, read only when a row carries both kind "short" and a story: YouTube gets the full story as its description with the source line, the room link and the tags; Facebook and Instagram get the same, cut to each network's own limit with the tail (the link and tags) kept intact; Threads gets the short hook and payoff pair with the link, cut to its own limit; Pinterest gets a title from the hook, a short pin text from hook and payoff, and its own board. Telegram, LinkedIn, X and Reddit are untouched, they already had a shape that fits a short's shorter fields.
2026-09-16 | films | edited | tools/films/shortmanifest.py | A finished plate film (a brief in tools/films/briefs/plate-*.json with its two rendered mp4s) never became a row on the reels shelf: the script only ever read the old know.json cards. It now also reads every plate brief, and for each one with a rendered tall film writes a "short" row with a caption built from the brief's own hook, payoff and source line, a long form story built from the library's own page text (parsed straight out of heroes.html, the person's own biography paragraphs or, for a brief about several people, the topical card whose words overlap the brief's own words the most), and the room link the brief now carries. A brief with no rendered film yet is skipped, quietly, not treated as an error. A slug that has a plate brief, rendered or not, no longer falls back to the old know.json path, so the fifteen older shorts keep moving to the plate path one by one as each is staged, without ever touching the FROM_CARD or OWN tables that name them.
2026-09-16 | films | edited | tools/films/publish-shorts.sh | Uploads each rendered tall short, and its wide pair when one exists, to the blob store under reels/<slug>.mp4 and reels/<slug>-wide.mp4, then merges the manifest rows shortmanifest.py wrote into out/index.merged.json, replacing a row of the same id in place and appending a new one, every other row on the shelf untouched; when reels/home.json exists it also writes out/home.merged.json with the shorts count and a fresh written mark. Prints what the owner still carries to main by hand, since this house never pushes from here.
2026-09-16 | films | edited | tools/films/briefs/plate-{sieve,zero,gold,road,teachers,licence,hospital,roundearth,sextant,darkroom}.json | Each of the ten older plate briefs (staged before the room field existed) gets one room line added after its note, naming the exact heroes.html anchor its film is about; nothing else in any of the ten files changed. Lets shortmanifest.py build a story and a room link for all ten, the same as the newer briefs already carry.
2026-09-16 | films | edited | tools/films/web/behave.js | Two bugs in the browser motion verbs. DO.count matched its comma stripped digits to find where a run of digits starts and ends, then sliced those positions out of the original, comma bearing string, so a count ending at "7,275" was rebuilt as "7,277,27575"; it now slices from the same stripped string it matched against. DO.glow ignored its own `for` duration and kept breathing, brighter and dimmer, for as long as the film ran past it, because its loop was keyed to the whole timeline's span, not the cue's own; it now runs exactly three tweens sized to a third of `for` each, fade in, hold, fade out, the same explicit, one shot shape every other verb here already uses.
2026-09-16 | tests | added | tests/behave.mjs, tests/shorts.mjs | tests/behave.mjs loads web/behave.js as a script (no browser, a hand built element and timeline stand in) and checks glow honours `for` and stops breathing once a short film ends, and count rebuilds several comma bearing numbers correctly. tests/shorts.mjs checks the row shortmanifest.py builds for a scaffolded plate brief (caption, story, room, tags), the merge publish-shorts.sh's own script performs into a manifest, reelRoom sending a short to its own room, and how each network in api/_channels.js shapes a short's story, source and room link. Both added to the SUITES list in .github/workflows/tests.yml.
2026-09-16 | tests | edited | tests/reels-kinds.mjs | New section for the four day afternoon split: Sunday, Tuesday, Thursday and Saturday ask for "short", the other three days keep the old sequence, and an empty shelf still falls back exactly as before on every day.
2026-09-16 | films | added | tools/films/TEMPLATE.md | The base template every NOOR film follows, locked by the owner on the dark room: the floor a film must meet to ship (the library's own plate, no parallax, one slow move per sentence, focus on the named element, captions from blur, no synthetic voice, marks on the drawing's events, the golden ratio layout, the watermark, both shapes at 30 fps, the audit green) and the scene architecture (hook, place, the mechanism one visible thing a sentence, the turn, the payoff, the source).
2026-09-16 | films | edited | tools/films/briefs/plate-{sieve,zero,gold,road,teachers,licence,hospital,roundearth,sextant}.json, tools/films/films/short-*.json | The nine researched films staged to the template: steps mapped to sentences in the drawing's own order, a travelling light or a glow on the element each sentence names, marks on the arrivals; every line checked by a still in both shapes; three label cuts and one garbled count fixed along the way. Words and facts untouched.
2026-09-16 | social | added | api/social.js, admin2.html | K_POSTED_CH only grows forward from a real send, so a reel that went ok before this fix shipped, or whose record nothing has touched since, has no field yet and still falls to the ledger. A new console action, backfillposted, walks the last 21 days of reel slots once, 126 reads at most, and writes the missing fields for every record with an ok result on any network; bounded, owner only, safe to run more than once. Added to the System room as Backfill the posted hash, with a Run button.
2026-09-16 | infra | edited | vercel.json | api/social.js carried no memory setting, so it ran at Vercel's platform default (checked: the entry held only maxDuration). Given 1536 MB now, since reading a whole reel's bytes into memory for Telegram's multipart upload (added the same day) is real weight the function did not carry before.
2026-09-16 | social | edited | api/_telegram.js, tests/telegram.mjs | Two more shapes were being uploaded to Telegram as if they were a whole reel: a body shorter than the content length the door itself declared, meaning the connection dropped partway, and a body under 100 KB regardless of what its length claimed, too small to be a real reel. Both are read as a fetch failure now, retried next hour, never handed to Telegram as a file.
2026-09-16 | social | edited | api/social.js | opts.force, already the way every other already sent refusal is skipped, now reaches sendOne too: the duplicate guard (findDuplicate) is skipped when a caller passes force, the same as the slot state and repeat checks above it, instead of refusing a deliberate resend the way it refused an accidental one.
2026-09-16 | social | edited | api/social.js, admin2.html, SOCIAL_ENGINE.md | The duplicate guard's ok result was marked dup:true, the same field lateArrival uses for a late flight's real answer kept beside a newer result; one channel could in principle need both meanings at once. The guard's flag is already:true now, with id, url and at, dup stays the list, and the console and doc are updated to match.
2026-09-16 | social | edited | api/social.js | A Facebook cut before the start phase even answered, no id ever learned, was recorded plain late and resent whole next hour, even though the request itself was still running underneath and Facebook might still take it. It is recorded pending now too, fb null with a since mark, and the next run's finisher looks for it by name on the page's own recent videos, title or description matching the record's caption and created after the cut, before giving up into a plain, healable failure after 30 minutes with nothing found.
2026-09-16 | social | edited | api/social.js, admin2.html | finishPendingReels and retryChannel both replace a channel's result wholesale, which was erasing dup and dupWarn the moment either one next touched that channel. Both fields now carry forward across every write to a channel (carryDup), and the console's duplicate badge is shown for whatever state the channel is in, not only inside the ok branch.
2026-09-16 | social | edited | api/social.js | PENDING_MAX_MS was giving up any stuck pending after two hours into a healable failure, including a Facebook upload with a known, real video id, which let the healer start the exact second upload this whole change exists to prevent. A Facebook pending with a known id now becomes fatal and unverified instead, the id kept on the record and never healed; every other pending (Instagram, Threads, a Facebook cut with no id yet) still gives up into a healable resend as before.
2026-09-16 | social | edited | api/social.js | findDuplicate read only the posted ledger, which needs every live network ok before it names a reel, so a reel ok on Facebook while Telegram was still failing (the 14 September shape) was invisible to the guard and got a second Facebook upload. A second hash, one field per reel and channel, written on every single ok, is checked first with one HGETALL, before the ledger; no extra slot reads.
2026-09-16 | tests | edited | tests/telegram.mjs | Rewritten for the multipart upload below: the stub fetch now answers a video's own url with bytes and a content length, then the Bot API call as the FormData Telegram receives; the old "file is too big" answer FROM Telegram is replaced with this module's own 50 MB skip, checked and refused before Telegram is ever called.
2026-09-16 | tests | added | tests/dup.mjs | The flight id path (a Facebook send the clock cuts after the upload has an id is recorded pending, not healable), the late answer kept beside a newer result rather than dropped (dup, dupWarn), the duplicate guard refusing a second send of the same reel id to a network within 21 days while a different reel still sends as usual, a transient status read staying pending rather than turning fatal, and the ledger's rule (every live network ok, or skipped for a reason that will not change, retires a reel; one still failing for real does not).
2026-09-16 | social | edited | api/_telegram.js | Telegram fetching a reel by its own url failed the same way twice, first the store's signed link and then this house's own door (api/reel.js), both "failed to get HTTP URL content" on every reel of every day: Telegram's own fetch is on ITS clock, not this house's, and two hops to reach a reel outran it far more than not. The module now reads the video itself, on the five minutes the run is given, and uploads it to Telegram directly as a file, the way a person attaching a video would; anything over Telegram's own 50 MB ceiling for a file handed to it this way is skipped, in words naming the size, rather than attempted and left to fail.
2026-09-16 | social | edited | api/social.js, admin2.html | A Facebook reel the clock cuts now carries its upload's own id: postFacebookReel writes it to a flight the moment the start phase answers, before the upload and the finish poll. A cut with the id known is recorded pending, not late with nothing to show, so healable refuses it outright and the healer leaves it for the finisher instead of starting a second, real upload over the first, which is where the doubled Al-Basit and Az-Zumar reels of 14 September came from. lateArrival no longer discards a late flight's real answer once a newer result has landed on the same record: the older id and url are kept beside it in results[ch].dup, with dupWarn set, and the console now shows a duplicate badge for it. Before any reel leaves for a network, the posted ledger, and, when it names a day, that day's own reel slots, are checked for the same reel id already answered ok on that channel; a match is answered ok, marked dup:true, with the earlier id, url and slot, and nothing is sent. The posted ledger's own rule (every live network ok, or skipped for a reason that will not change) was already correct and is unchanged; Telegram's fix above is what was actually holding it at zero.
2026-09-16 | films | edited | tools/films/web/plate.js, tools/films/web/film.html, tools/films/shortmusic.py, tools/films/audit.py, tools/films/films/short-darkroom.json | The plate short polished on the owner's notes: parallax off for the illustration (every stroke on one plane, a ray's end always on the flame and the far wall; a brief may ask for it back), one slow continuous camera move per sentence (inOutSine across the sentence, no pre arrival, no sway, no closing push; a sentence that adds no step holds with a slight pull back), the rack focus slower and deeper (42 percent, 1.2 s), the picture area and the word band on the golden ratio division of the frame, the score's struck marks on the drawing's own events (a ray setting out and arriving, the hole lighting; the list written beside the wav), and audit.py able to audit a plate short (its lines, its frames at 30 fps, the muxed film's loudness, the marks against the events).
2026-09-16 | films | edited | tools/films/web/stage.js, tools/films/web/film.html, tools/films/plates.sh, tools/films/films/short-darkroom.json | The owner's verdict on the night's proof (a machine voice, a 12 fps proof, a 3D room that showed no wall when the words named one): the dark room is made from the site's own darkroom.svg plate with the plate engine instead. The captions of a plate short fade in slowly from transparent and blurred to sharp (1.1 s, the first line 0.7 s) and leave into blur (0.7 s); every short carries the watermark; plates.sh renders both shapes at 30 fps with a three sample shutter and lays the score under each; no synthetic narration in any NOOR film. The scene film and the 3D room stay in the tree behind the scene flag, verified not to touch the plate shorts.
2026-09-16 | films | edited | tools/films/web/lume.js, tools/films/film.py, tools/films/specs/darkroom.json, tools/films/films/darkroom.json | Round six of the dark room after the audit of the rendered proof: the lamps are candles and flicker from seeded noise (the lamp, its ray and its image together), a scene's camera may carry an end station so the last scene pushes in instead of standing still, the compiler copies it. The audit's pacing meter still reads the last scene as quiet (2.6 against 4); left open for the owner's judgment.
2026-09-16 | films | added | tools/films/specs/darkroom.json, tools/films/film.py, tools/films/filmsound.py, tools/films/audit.py, tools/films/films/darkroom.json, tools/films/films/darkroom.envelope.json | The masterplan's film spec and the terminal pipeline: one JSON per film (research with sources, story, figure, scenes with words, camera, events, lantern, sound and narration, music, watermark, export); film.py compiles it into the chapter the stage renders (retimed to the spoken lines), speaks a stand in narration with Piper and writes the envelope the light breathes to, builds the sound (score window, roomtone, a struck mark on every sound event, the bed ducked under the voice, master at minus 14 LUFS and minus 2 dBTP, exactly the picture's length), renders through noor.py and muxes; audit.py checks the result against the production rules (no dash on screen, a change every 2 to 4 s, first frame, no black or blown frame, watermark, safe area, loudness, marks on their events, lengths agreed).
2026-09-16 | films | added | tools/films/web/lume.js, tools/films/web/stage.js, tools/films/web/film.html | The scene film: a chapter with a scene object puts one figure as the whole world, the camera at explicit stations (eye, look, hfov converted per frame shape, hold before the aim turns, a glide between stations) and the light on explicit cues (hold, then travel; dimmed near the lens so it never floods the frame). The darkroom figure kind: a room the camera stands inside, an opaque back wall with a real hole, lamps outside, rays drawn through the hole, images at the computed inverted point on the far wall, out rays for the old idea, every state replayed from an events list. The watermark (the house mark and noorcodex.com) inside the safe margins on every frame. Headline, eyebrow, sub and source of every beat drawn in the band in scene mode (the first proof lost the headlines to the hidden pane; found by the refuter). Corridor films untouched, verified pixel identical.
2026-09-16 | films | edited | tools/films/ (the engine's own files: noor.py, contact.py, render.py, plate.js, behave.js, plates.js, shortplate.py, shortmusic.py, briefs, films) | The complete engine the audit mapped is now in the repository, not only on the owner's Mac (main held a two day old subset); music, plate sources, voices and renders stay out.
2026-09-16 | house | edited | VIDEO_ENGINE.md, CURRENT_STATE.md, HANDOFF.md, DECISIONS.md | The engine document rewritten for the rebuilt engine and the dark room prototype; the state, the hand over and the decisions brought to the night of 16 September.
2026-09-16 | house | added | CLAUDE.md, CURRENT_STATE.md, ARCHITECTURE.md, DECISIONS.md, HANDOFF.md, VIDEO_ENGINE.md, SOCIAL_ENGINE.md, .claude/agents/*.md, .claude/skills/*/SKILL.md | The Director's standing orders and the five specialist roles (scout on Haiku, researcher and builder on Sonnet, refuter on Opus, debugger on Fable), the seven workflows, and the six state documents that let a new session start from the state and not from memory. Kept in the repository, out of the deployment.
2026-09-16 | shell | edited | api/page.js, noor-fx.js, index.html, scripts/gen-dictionary.py, characters.html, companions.html, places.html | The shell's cache buster moves to 14 because noor2.js and noor2.css changed (the skip link, the focus held in the sheets); the hub script carries a version too. Found by the refuter's review of batch 5.
2026-09-16 | site | edited | index.html, kids.html, legal.html, tests/rooms.mjs, tests/home2.mjs | The home's footer and library list and the kids page send schools to /school, not /license; the legal page's descriptions no longer name licensing; the static sitemap is 607 pages.
2026-09-16 | site | removed | license.html, api/license.js, vercel.json, noor-fx.js, assets/menu-index.json, scripts/nav49.py, scripts/synergy.py, sitemap.xml, llms.txt, assets/search-index.json, api/assistant.js, api/marketing.js, api/sponsor-checkout.js, madrasa-data.js, legal.html, madrasa.html, feedback.html, donate.html, school.html, masjid/index.html, masjid/setup.html, masjid/start.html, tests/api-audit.mjs, .github/workflows/remove.yml | The licensing door and its Stripe subscription function are gone: /license and api/license.js are deleted from the tree, every old address that pointed at them (the menus, the sitemap, llms.txt, the search index, the API assistants and the pages that named the licence) now reads true without a subscription, and every visitor who follows an old /license link is redirected, permanently, to /school, where a school or an organisation is told the Codex is free to use directly, no subscription, with a gift welcome at /donate. A new workflow, remove.yml, opens the pull request that deletes files by hand from the Actions tab, since the house's own upload page cannot delete one.
2026-09-16 | rooms | added | api/page.js, api/sitemap.js, vercel.json, .vercelignore, assets/entity-graph.json, noor-hub.js, assets/hub.css, prophets.html, allah.html, characters.html, companions.html, places.html, words.html, assets/search-index.json, scripts/gen-dictionary.py, tests/rooms.mjs | Four families of rooms rendered from the data the hubs already read: /prophet/<id> (25, prophets-data.js), /companion/<id> (58) and /character/<id> (40 angels, jinn, animals and end time figures, both from characters.js), /place/<id> (34, places.js) and /name/<n> (the 99 Names from allah.html's own NAMES list). About 46,000 sourced words that showed only in a modal on tap now have a title, an address, a description, Open Graph, JSON-LD (Person, Thing, Place, DefinedTerm in the set of ninety-nine), the Arabic, the account, the facts, the Qur'an and the narrations with their sources, and a Read beside it shelf from the content graph's edges at confidence 0.8 and above: the word for the same entity, the Lights and chapters that name it, people and places named either way, the stories written from a Name (a Light names a person only by a whole name, since a given name alone tied Mansa Musa to the prophet Musa). No room carries a picture. Every hub tile and every opened modal links the entity's room, the search index's 256 entity rows point at the rooms instead of a hub anchor, the rooms sitemap lists all 256, and the function bundle includes the five files the rooms read.
2026-09-16 | tests | added | .github/workflows/tests.yml, tests/rooms.mjs, tests/clock.mjs | On every push to main, and by hand from the Actions tab, Node 22 runs the 36 suites that need no browser, no server on a port and no secret, each under a 90 second cap, and the run fails if any one fails; the suites that need playwright, a server or a neighbouring checkout are named in the file with the reason. tests/rooms.mjs renders through a copy of the tree without reels/, so its manifest stub holds again (four verse assertions were reading the live manifest from disk), and tests/clock.mjs answers the Facebook status read the poster makes since 15 September.
2026-09-16 | build | added | .github/workflows/generators.yml | A drift guard, weekly and by hand: the six generators that reproduce their output (gen-lights.py, gen-quran-study.py, gen-dict-index.py, build.mjs, build-questions.py, gen-dictionary.py) run in the checked out tree and the job fails if git status is not clean afterwards, with each drift's diff stat in the job summary. The fifteen page generators the audit found drifting from their pages, and the four other scripts that rewrite what they generate, are listed in the file with their drift and stay out until they are fixed.
2026-09-16 | performance | edited | api/page.js, mizan.html | The first screen of a Light, a chapter, a verse, a surah, the day and each new room ships with n2-in, and the first card of Two Lives with visible, the way the home and the shelves ship theirs: the shell held the first words at opacity 0 until the script arrived, 1.3 seconds of blank night on a desktop that paints the home in 144 ms.
2026-09-16 | performance | edited | tools/reels/render_missing.py, index.html, vercel.json, .github/workflows/reels.yml, tests/home2.mjs | The home page chose one verse by downloading the whole reels manifest, 181 KB gzipped and three fifths of its weight. The render run now also writes reels/home.json (the verse rows alone, with only the fields the home reads, and the count of shorts on the shelf), the home reads that file and falls back to index.json until the next run has written it, and both manifests are kept five minutes at the edge and revalidated in the background.
2026-09-16 | accessibility | edited | assets/noor2.js, noor-fx.js | The two sheets that say aria-modal now behave as one. The shell's sheet is named by its first line, focus goes in when it opens, Tab stays inside it, and focus returns to the door that opened it when it closes; the More sheet does the same, where a Tab from its field walked out into the page behind it and Escape dropped focus to the top of the document.
2026-09-16 | accessibility | added | api/page.js, assets/noor2.js, assets/noor2.css | Two skip links first in the body, off screen until one is focused and then a pill in the shell's own tokens: to the content, and to the five door bar, which is last in the document and from a keyboard came after every link on the page (Tab 70 on the home, past Tab 80 on the dictionary).
2026-09-16 | animation | edited | allah.html | The field of light behind the 99 Names is drawn once and left under reduced motion, drawn at most thirty times a second with motion, and not at all while the tab is hidden; it redrew the whole screen every frame for as long as the page was open, reduced motion or not.
2026-09-16 | animation | edited | prophets.html | The 220 twinkling stars of the unnamed, each its own infinite CSS animation (231 running animations at idle, the most style recalculation on the site), are one canvas drawn at most thirty times a second while the field is on screen and the tab is visible; under reduced motion 120 stars are drawn once and left.
2026-09-16 | dictionary | edited | dictionary/*.html (523 pages) | The word pages rebuilt by the dictionary workflow from the corrected generator and merged (pull request 13): shell v13 with the More door, and on 147 pages a "Rooms that name it" shelf with 275 doors to a Light, a chapter or a surah.
2026-09-16 | social | edited | api/social.js, api/_schedule.js | Read from the live records: Telegram never received a reel ("failed to get HTTP URL content" on the store's signed link), so it is handed the house's own door /api/reel?id= (video/mp4, no redirect); a repair is pinned to the reel the record names, so a retry after the shelf grew is no longer refused as "a different card" (every reel of 15 September was); a reel refused as drift is tried again; an Instagram story container "not ready for publishing" (code 9007, the dusk slot every day) is asked again four seconds apart instead of failing.
2026-09-16 | reels | edited | tools/reels/plan_build.py, tools/reels/plan.json | A verse reel's caption ends at the verse's own room, noorcodex.com/verse/<s>-<a> (10:6 is /verse/10-6, 1:1-7 is /verse/1-1-7); all 600 sent the viewer to /quran. A Light's caption ends at noorcodex.com/light/<id> and a Did you know's at the Light it came from; 304 carried the bare domain though every one of their Lights is a room. Word captions keep their page. Only captions changed; copy_audit.py reads the rebuilt plan with 0 findings on 1,565 cards.
2026-09-16 | navigation | edited | noor-fx.js, assets/menu-index.json, scripts/nav49.py, index.html, quran.html, tests/home2.mjs, tests/anime.mjs, tests/relabel.mjs | The map of the house (the More sheet on every page, the home's library list, the header generator) sends The Path to /path instead of /#timeline and names Today, The Lights and The Verses under The Story: apart from the home no static page linked the rooms api/page.js renders. On the Mushaf each surah's heading links its own room, /surah/<n>. Three tests count the rooms from the menu index instead of the number 42.
2026-09-16 | dictionary | edited | scripts/gen-dictionary.py, noor-fx.js, tests/dictionary2.mjs, tests/search-index.mjs | The 523 word pages are generated at shell v13 with the More door, as every room is; the pages themselves arrive through the dictionary workflow's pull request, so noor-fx.js keeps its runtime patch for the retired Search door until then, and the patch now stands down on a page that already carries the More door instead of fetching the search on every room view. Each word page prints up to three rooms that name it (a Light, a chapter, a surah whose own text names the word, the reverse of api/page.js relatedWords()) on the shell's shelf: 147 words gain 275 doors into the library where they linked nothing but other words.
2026-09-16 | search | edited | assets/search-index.json, scripts/gen-dictionary.py | The site search carries every reader room (/allah, /muhammad, /mizan, /three-lives, /dictionary, /light, /path, /today and /verses were missing) and the rooms api/page.js renders: the 350 Lights by title, the 114 surahs by name, number and meaning, the 99 Names by transliteration and Arabic, and the 71 chapters at /path/<n>, where they pointed at /#path-flow, an anchor the rebuilt home no longer has. /404, the noindex /masjid/board and the retired /license are no longer offered. Every other entity and room stays: 1,746 entities in 19 groups, 74 rooms, 134 KB gzipped.
2026-09-16 | dictionary | edited | scripts/gen-dictionary.py, .github/workflows/build-dictionary.yml | The generator behind the dictionary workflow rewrote assets/search-index.json from {w, r} alone, losing the 1,183 prophets, companions, places and surahs, and put the hub back on shell v2 with the retired Search door, on every change to a word. It now reads the shipped index first and keeps every group, entity and room it carries, rebuilding only what the library's own files can build, and links the shell at v13 with the More door: run on the tree it reproduces dictionary.html byte for byte. A guard step before the pull request opens fails the run if the index lost a key, a group or more than five percent of any group's rows, or if the hub lost data-n2, the More door or the shell version api/page.js carries.
2026-09-15 | social | edited | api/social.js, tests/crongate.mjs | With CRON_SECRET set, the hourly cron is admitted by its bearer only; a user agent or a signature header, which anyone can type, no longer opens the door.
2026-09-15 | social | edited | api/_channels.js, api/_threads.js, tests/claim.mjs | A reel caption cut to 500 for Threads and Pinterest keeps its closing line and its tags and cuts the body instead (148 captions lost "noorcodex.com" and every tag); the house's own tag survives a network's tag limit.
2026-09-15 | social | edited | api/_schedule.js, api/social.js, tools/reels/render_missing.py | Every reel now links its own room (a verse to /verse/…, a word to /dictionary/…, a Light to /light/…, a Did you know to the Light it came from once the manifest carries src); the cards link /today?date=, not /?light=, which no page reads. Pinterest pins carry the room as their destination.
2026-09-15 | social | edited | api/social.js | A slot whose every network refused is settled and healed one network at a time with its counters, instead of being composed and sent whole again every hour.
2026-09-15 | social | edited | api/social.js | A request the clock cut is no longer forgotten: its answer, when it arrives, is written onto the record over the "late", so a reel that landed after the cut is never sent a second time. A PUBLISHED Instagram container is read as published, not pending (it was given up on after two hours and sent again).
2026-09-15 | social | edited | api/_lights.js | The Lights library is read from the site's own name, never from a deployment address: Vercel Authentication walls the *.vercel.app addresses and the hourly cron arrives with one, so the day's card slot found the library unreachable every hour from noon to dusk and the day's card story never went out.
2026-09-15 | social | edited | api/social.js, tests/claim.mjs | A Facebook reel is asked what became of it after "finish": ready with its permalink is sent, still processing is pending for the finisher, an error is refused in Meta's words. A YouTube upload kept private (the API audit) is uploaded, not published, and does not retire the reel. A new test covers the claim, the status read and the private Short.
2026-09-15 | console | added | changes.txt, admin2.html | This archive, and a card in the System room of the console that reads or downloads it.
2026-09-15 | mobile | edited | 18 pages at the root and 14 kids games | Removed user-scalable=no and maximum-scale=1 from the viewport tag so readers can pinch to zoom; body text on those pages is 12 to 14 px. The Mushaf (quran.html) is among them.
2026-09-15 | languages | edited | noor-fx.js | A translated door (/ar, /ur, /fa, /prs, /pa, /ps and the rest) keeps its own lang and direction; the script used to write lang=en dir=ltr over the Arabic door for every first visitor two seconds after it rendered right to left.
2026-09-15 | search | edited | api/page.js | The BreadcrumbList on every rendered room now carries @context, so search engines read the 1,138 breadcrumbs it always printed.
2026-09-15 | social | edited | api/_channels.js | A Pinterest video refused under Trial access is recorded as waiting, the same stage the pin call already recognised, instead of as a fault the healer retried forever.
2026-09-15 | social | edited | api/_youtube.js | The daily YouTube cap is 6 to match the six reel slots a day; at 5 the sixth reel of every day was refused as over quota.
2026-09-15 | social | edited | api/_schedule.js, index.html, tests/home2.mjs | The afternoon slot asks for a short and, while the shelf has none, takes the old afternoon rota for that weekday and counts its steps; "short" with no rows fell through to a verse every afternoon, 24 verses a week for 19 planned.
2026-09-15 | social | removed | api/warm.js | The nightly run no longer calls runDaily: it composed the day's card at 04:00 and, in auto mode, posted it to the Facebook and Instagram feed, ignoring the cards are stories only rule, then the noon slot sent the same card as a story.
2026-09-15 | social | edited | api/social.js | A slot is claimed in the store (SET NX, ten minutes) before anything is sent, so two runs in the same minute cannot both send it.
2026-09-15 | social | edited | vercel.json, api/social.js | The poster has five minutes instead of sixty seconds (maxDuration 300, RUN_BUDGET_MS 280000): the function log showed Instagram taking 35 to 42 s of every run, Facebook then cut as late, and Threads, Telegram and Pinterest never reached, which left every reel slot partial and the ledger of posted reels at zero.
2026-09-15 | reels | edited | reels/index.json, reels/verse-10-6.json, reels/verse-39-61.json | Verse 10:6 and 39:61 rendered again under the new ceiling and merged (run 13, pull request 12). The shelf lists 1,457 reels with video.
2026-09-15 | reels | edited | tools/reels/sound.py, tools/reels/webreel.py, tools/reels/render_missing.py | The recitation of a verse reel is held by a lookahead limiter at -3 dB true peak and encoded at 128k; two reels a run were clipping after the AAC encode and being removed by the sound check. A removed reel is now named on the run page.
2026-09-15 | reels | edited | reels/*.json, reels/index.json, tools/reels/posted.json | Monday's run (run 12) merged: 478 new reels with their sidecars and covers, videos on GitHub Releases (pull request 11).
2026-09-16 | films | edited | tools/films/shortmusic.py, tools/films/audit.py | The sound law: at most eight struck marks a film (the hook's own first step, one arrival per sentence with a travelling light, the payoff's arrival or first step), never two within 1.6s and arrivals win the tie, at minus 38 dB against the music instead of minus 29.5 in a longer, wetter room (6.5s, wet at minus 40) so each one lands as a distant touch and not a strike, felt the default voice now. short-sieve fell from a dense stream to 4 marks, short-asabiyya to 2, both measured about 21 dB under the music.
2026-09-17 | films | added | tools/films/blobput.py, tools/films/test_blobput.py | The Mac's uploader for the shelf: python3 and curl only (the Mac has no node), the exact PUT @vercel/blob 2.8.0 makes (read out of the package: vercel.com/api/blob, api version 12, the store id from the token, access public, no random suffix, overwrite allowed, a year of cache), the token on curl's standard input and never in argv or a file, a 5xx retried four times and a 4xx final, proved against a local stand in shelf by test_blobput.py, which tests/shorts.mjs runs.
2026-09-17 | films | edited | tools/films/publish-shorts.sh, tools/films/shortmanifest.py, tests/shorts.mjs, tools/reels/README.md, tools/films/README.md | The publish step runs on the Mac where the films are: uploads with blobput.py; reads the shelf, the cards and heroes.html from shelf/ when the site's tree is not around it (index.json, know.json, site/heroes.html travel with the folder and are refreshed from main); remembers each successful upload in out/uploads.json so a rerun sends only what is new; merges ONLY rows whose upload succeeded (the fourteen older know.json shorts have no film in the folder and were about to reach the shelf with 404 urls); writes the merged shelf under out/main/reels/index.json with its real name so out/main can be dragged onto the upload page as it is.
2026-09-17 | films | edited | tools/films/blobput.py, tools/films/publish-shorts.sh | The owner's first real publish run made all twenty two delivery copies, fourteen minutes of ffmpeg, and only then found that not one upload would be accepted: the shelf's answer was never looked at until the first file was already encoded, and the run ended with a full out/deliver, an empty out/uploads.json and a shelf file carrying no film at all. blobput.py grows a probe (twelve bytes to reels/__probe.txt) which publish-shorts.sh now runs BEFORE any encoding, printing what the shelf said and stopping there; every failed upload's own words are kept in out/upload-errors.log rather than scrolling past in a window nobody is watching.
2026-09-17 | lantern | edited | api/illuminations.js | The owner's screenshot of the Mushaf showed the Verse Lamp reading "...": the model had answered {"reflection":"..."} and the only test on a piece of writing was that the string was not empty. A piece of prose from the Lantern must now BE prose (enough real words, mostly letters, not one token repeated) or it throws to the hand written fallback, which is what a refusal already does, so no tile goes dark and none goes silly.
2026-09-17 | films | added | tools/films/briefs/plate-{starnames,loanwords,earthsize,astrolabe,canon}.json | The last five discovery films written from the plates on heroes.html that no film had taken: the Arabic star names, six Arabic words inside English, al-Biruni sizing the earth from one mountain, the astrolabe, and the Canon of Medicine taught in Europe for six centuries. Written from the cards' own paragraphs with a proof quote kept per line, then refuted: 28 corrections applied, all sourced (hardened dates where the page hedges, a film that never said the word astrolabe, a line the drawing does not carry, a sentence said twice).
2026-09-17 | films | note | tools/films/briefs/_not-films/ | Two closing drawings, the unfinished wall and the chain of lamps, were written as films and are not films: their cards name no person, no place and no date, by design, so neither can pass the template's own who, where and when gate without inventing one. The gate stands and the two briefs are set aside rather than the rule being bent for them.
2026-09-17 | films | edited | tools/films/publish-shorts.sh | The first film went out at 14:00 UTC and Instagram refused it seven seconds later ("could not process the video: ERROR") while YouTube took both shapes and Telegram took the file. The masters are rendered from full range frames, so the delivery copy came out yuvj420p, color range pc, level 5.0, with the score copied through at 252 kbps; the reels Instagram accepts every day are high profile yuv420p with aac at 48 kHz, 2 channels, 96 kbps (tools/reels/compile.py). The delivery recipe is now that same proven one, with the levels really converted rather than tagged (scale=in_range=full:out_range=tv), bt709 written on all three tags and the audio re-encoded rather than copied; it also comes out at 21 MB instead of 31. The recipe carries a revision (out/deliver/.rev) so copies made under the old one are remade rather than trusted.
2026-09-17 | films | added | tools/films/briefs/plate-{starnames,loanwords,earthsize,astrolabe,canon}.json, tools/films/films/short-{starnames,loanwords,earthsize,astrolabe,canon}.json | The five staged: steps mapped to each plate's own drawing order, four to six motions a film, every selector proven against the svg, 90 stills read one by one in both shapes. 41 briefs now exist, 22 rendered and live, 19 waiting on the owner's Mac.
2026-09-17 | films | edited | tools/films/publish-shorts.sh | The owner ran the publish step after his rendered masters were no longer in the folder. The upload loop never ran once, shortmanifest could build no row for a film with no file, and the run ended in under a minute having quietly written a shelf with one film on it instead of twenty two: one drag away from taking twenty one films off the live site. Three things now stand in the way. The count of rendered masters is checked first, before the key and before the network, and a folder of briefs with no film says so in one second and names ./plates.sh. The merge reads out/uploads.json, the ledger of every upload that ever succeeded, which the comment always claimed it did and the code never did. And a run that would leave the shelf carrying fewer films than it already carries, measured against both the shelf being merged into and the last shelf this folder produced, refuses and writes nothing. Proved against both failure shapes.
2026-09-17 | films | note | the owner's render folder | The 44 masters came back from the Trash, thirteen of them with the deletion time inside the name (short-algebra-tall-30fps 10.09.12.mp4). The glob in plates.sh, publish-shorts.sh and shortmanifest.py is exact, so those thirteen were invisible to every one of them and the run would have refused a second time. Renamed in place and all 44 probed whole: right durations, an audio stream on every one.
2026-09-17 | films | edited | tools/films/publish-shorts.sh | The delivery recipe's revision moved out of a marker file and into every copy's own name (short-<slug>-<shape>.r2.mp4). The marker was written at the END of a run, so the run that encoded nothing at all still wrote it, the old copies were stamped with the new recipe's number, and the next run skipped all forty four as already current: the owner ran it and every file said "already on the shelf, unchanged" while still carrying the encoding Instagram refuses. A file on disk cannot say which recipe produced it, so the recipe is now part of the name, where nothing else can claim it.
2026-09-17 | site | edited | 42 old rooms (36 at the root and 6 under masjid/), noor-fx.js, scripts/oldrooms.py | THE DIET STUB WAS TELLING noor-fx A LIE. The old rooms' stub paints html and body dark so there is no white flash before the four sheets land. noor-fx.js decides whether a room is parchment (and so needs the generated night sheet turning its palette over) by reading that same floor's colour, so a stub-darkened floor read as a room written dark and html.n2-room was withheld from all thirty six at once: the ENTIRE night sheet stopped applying to them. Measured on the live site from the owner's own browser, the Qur'an page's 114 surah names sat at 1.23:1, ink #2C2416 on #0A1024, invisible; /hajj's "Make my plan" at 1.05. The stub now declares :root{--n2-parchment-room:1} and noor-fx believes a room that declares itself, keeping the colour guess only for rooms that say nothing. The floor is still painted, so the white flash stays fixed, and a reader with no JavaScript still gets the stub. Qur'an gate names 1.23:1 to 18.67:1; elements under 4.5:1 on that page 51 to 2.
2026-09-17 | site | note | stories/*.html | Batch 6's own list is 51 rooms: 36 at the root, 6 under masjid/ and 9 under stories/. The nine stories rooms never received the diet, which is what the nine standing failures in tests/rooms.mjs have been saying. They are not part of the contrast regression, since with no stub their floor stays light and the night sheet applies to them normally; what they lack is the lighter header, the noscript menu and the night painted before the sheets land. Incomplete, not broken.
2026-09-17 | films | edited | tools/films/publish-shorts.sh, tests/shorts.mjs, reels/short-*-cover.jpg (22) | A ROW COULD PROMISE A PICTURE NOBODY HAD MADE. shortmanifest writes cover: true into every film's shelf row, which sends the poster to reels/<id>-cover.jpg on the site, and nothing in this pipeline ever made that file. Instagram is the one network whose spec marks the image required, so on 17 September the first film went to YouTube, Facebook, Threads and Telegram and Instagram alone refused it with "could not process the video: ERROR", which reads like a bad encode and is not: the house's own pre-flight had already said "the card URL answered 404" and the console's diagnose panel said it again, correctly, to a screen nobody was watching at the time. The shelf carries 1,479 cards and 1,479 covers on disk; the 22 films were the only rows without one, and every one of them would have been refused in turn, four afternoons a week, until a person happened to look. The missing picture was never the fault. The fault was that a row could reach the live shelf promising a picture that did not exist, so the cover now travels the same road as the video: made from the master at four fifths of the way in, where the drawing is finished and the payoff line is on screen, uploaded to the same shelf by the same function in the same run, recorded in the same ledger, and held to the same law by the merge. No cover on the shelf, no row on the shelf. A film cannot now be posted anywhere before its cover exists, because it cannot be listed. When the film is one the shelf already carries, holding the row back would take it off the live site, so the run refuses and writes nothing at all instead. Proved in tests/shorts.mjs against the real merge extracted from the script: the merged row carries the uploaded cover url rather than the bare true, a film whose cover never arrived is not listed and is named aloud, and the same film already on the shelf stops the run with nothing written. A separate check reads the real shelf against the real folder and fails if any row promising a cover has none, which is the check that was missing; it was proved by hiding one cover and watching it name the film. The 22 covers also go to reels/ so the rows already live, which still say cover: true, are whole until the next publish run turns them into shelf urls.
2026-09-17 | films | added | tools/films/rescore.sh | Put a new score under films that are already rendered. The score is muxed at render time, so changing the music would otherwise mean drawing forty four films again, about two and a half hours, for pictures that were already correct. This writes the new score and swaps the audio track, seconds a film, keeping each master as it was under out/prescore/ so it can be run again against a different set of scores and twice does not stack two scores. shortmusic.py's own --mux looks for <slug>-<shape>-60fps.mp4 and the films are rendered at 30, so it has never found one.
2026-09-17 | films | edited | tools/films/rescore.sh, tools/films/music/ | THE SCORE IS NOW A VOICE. A viewer objected to the instruments under the films, so the two library pieces the shorts were cut from are out of music/ and a nasheed is in: voice only, wordless vowels, no instrument of any kind, six minutes, maqam nahawand in G minor at 50 bpm, which is the key and the length of the piece it replaces. All forty four masters were rescored in place, picture untouched, not one frame re-encoded, every one of the twenty two films drawing from the one new track and each from a different window of it. The old scores were moved rather than deleted, to _scores-with-instruments beside the render folder. AND THE SCRIPT THAT DID IT HAD THREE FAULTS OF THE SAME FAMILY, a step doing nothing and the next step believing it, which is the fault this whole day has been about. It trusted a wav it had not written, so when the score step failed it muxed one from the night before, made from the old music, and reported success for every film. It built the mux target by adding a second short- prefix to a slug that already had one, so every target was a file that did not exist, the test for it passed over in silence, and nineteen films were reported rescored while nineteen files were never opened. And it trusted the backup copy, one of which arrived truncated at 28 MB of 192. Each wav is now deleted before its film is scored and checked after; a film matching no master is an error that is named; and a backup made in the moment must match its master byte for byte or the master is not touched.
2026-09-18 | films | edited | reels/index.json | The shelf carries the voice and the pictures. The owner's publish run remade all forty four delivery copies from the rescored masters, made a cover for each of the twenty two films and put it on the shelf beside its video, and the merge wrote each film's row pointing at its own cover: 22 of 22, none left carrying the bare true that sent the poster looking for a file nobody had made. 66 uploads in the ledger, 44 videos and 22 covers. The diff is 22 lines, one per film, and nothing else on the shelf moved: 1,479 rows in, 1,479 rows out, every other kind untouched.
2026-09-18 | films | added | tools/films/web/behave.js, tools/films/briefs/plate-{crankshaft,astrolabe}.json | THE PICTURE NOW DOES THE THING THE WORDS SAY. The owner watched the al-Jazari film and asked why a short whose whole subject is rotation becoming a stroke shows a wheel that never turns. He was right, and it was not one film: the motion engine had nine behaviours, travel, flow, pour, trace, sweep, pulse, count, glow and orbit, and every one of them decorates a drawing that does not move. A light runs along a path, an element glows, a number counts, a mote goes round. Not one of them moved a part of a mechanism relative to another part, so no film in the library could show a machine working. Three behaviours added, and they compute rather than keyframe. slidercrank drives the mechanism al-Jazari drew and every piston engine still uses: the pin rides the circle, the rod holds its length, and the slider's position falls out of the right triangle the rod makes with the axis, which is why the stroke comes out faster on one half than the other exactly as a real one does. spin turns a body about a centre in whole turns. swing takes an arm about a pivot and brings it back. Every dimension is read out of the drawing at setup, so a figure redrawn to new proportions animates to its own, and nothing is hard coded. Each of them writes back the exact attribute strings it found when it finishes, because the behaviours rewrite the very d and cx the briefs select on: after one turn path[d="M188 110L320 150"] matches nothing and a second mechanism later in the same film would silently do nothing, which is this week's recurring fault in a new costume. It also means the plate at rest is the plate as drawn. Two films needed them and now have them: the crank, where the wheel turns, the rod swings and the piston strokes; and the astrolabe, whose line five says turn the star map to match the sky and whose star map now turns, on the centre pin, eccentric as a real rete is, with the sighting bar swinging to take a star's height. The other thirty nine were read and left alone: they are journeys, maps and tables, where a travelling light is the correct treatment and a moving part would be an invention.
2026-09-18 | films | edited | tools/films/shortmusic.py | A film now cuts from the top of its track. pick_window was written for the two instrumental pieces and hunts the passage that opens quiet and then grows, which is the shape a short wants and which it found well in them. On the nasheed that replaced them it hunted badly: it put every film at 246 seconds in, and the owner listened and said plainly that it had taken the worst part of the track and the beginning is the good part. A rule that scores loudness cannot hear that and the person who can has spoken, so 0 is the default and --pick restores the hunt for the piece it was written for. Proved both ways against the nasheed: the default reports from 0 s, --pick still reports from 246 s.
2026-09-18 | films | edited | tools/films/rescore.sh | A BACKUP OF A DIFFERENT PICTURE IS NOT A BACKUP. rescore.sh keeps every film's picture under out/prescore so the music can be changed again later without stacking two scores, and its rule for reusing one of those backups was that the file existed, which is true only while the master is still the one the backup came from. Every backup sitting there tonight was made the day before the mechanism engine landed, when nothing in those films moved, so a rescore run against the current masters would have copied the older pictures forward, thrown away every moving part in the library and reported success for all forty four. Each backup now carries a stamp of its master, the length and a checksum of the first megabyte, which with faststart is the index of the whole file and differs between any two encodes; a master that no longer matches its stamp is refused by name, per shape, with the remedy printed, and an unstamped backup is refused for the same reason, because nothing can vouch for it. The stamp is taken with wc, dd, cksum and cut rather than stat, because BSD stat reads -f as a format string and GNU stat reads it as describe the file system: the first attempt used stat, wrote a page of block counts into the stamp on one of the two machines, and so the stamp never matched itself and every film was refused. Four cases now run end to end: a fresh master scored and stamped, an unchanged master scored again, a master re-rendered behind its backup refused while its untouched sibling shape still passes, and a cleared out/prescore starting over.
2026-09-18 | films | edited | tools/films/publish-shorts.sh, tests/shorts.mjs | A COVER IS A FRAME OF A MASTER, SO IT IS ONLY AS CURRENT AS THE MASTER. make_delivery_copy has always remade its file when the master was newer; make_cover, written in a hurry the day Instagram refused the first film, skipped whenever a file of the right name was there at all. Two makers in one script, one careful and one not, and harmless for exactly one day: the covers were cut on 17 September and every master was rendered again on the 18th, this time with its mechanisms moving, so a publish run would have remade all forty four delivery copies, seen twenty two covers already sitting in out/deliver, kept them, and sent twenty two films to the shelf and from there to five networks behind stills of the versions where nothing moved, with nothing anywhere reporting a fault. The delivery copies would have been right and the pictures announcing them wrong. make_cover now uses make_delivery_copy's rule, in the same words. The test does not take the script's word for it: it lifts every make_*() out of the shipped file and requires each to guard on the master's age rather than on a file merely existing, so a third maker added later cannot quietly repeat this, then lifts make_cover itself out and runs it, a cover made, kept on a second call, and remade after the master is replaced underneath it. Reverting the guard fails both halves, which is the only reason to trust either. 157 checks to 164.
2026-09-18 | films | edited | tools/films/publish-shorts.sh, tests/shorts.mjs | A KEY TYPED IS A KEY KEPT, so it may now come from a file. Publishing the shelf has always meant typing an export of BLOB_READ_WRITE_TOKEN into a shell, which writes the key into the history file in plain text where it stays until something clears it, and the key that published this shelf had to be replaced this week for a related reason: replacing a key by typing it is how the next one goes the same way. If the variable is not already set, publish-shorts.sh now reads the key from the first line of blob-token.txt in the folder above it, which is the same place and the same habit as github-token.txt, with whitespace stripped, the file tightened to its owner on the way past, and nothing of the key printed. Exporting by hand still works and still wins, so no existing habit breaks. The check that used to read "the token is never assigned in the script" failed on this, correctly, because it was written when the environment was the only road and what it always meant was that no secret literal sits in the file; it now says that instead. Every assignment's right hand side must be a substitution reading blob-token.txt, no string shaped like a key may appear anywhere including the comments, the variable is never echoed, and the file is chmodded. Then the chunk that finds the key is lifted out of the shipped script and run against a stand in with the variable cleared: the key arrives with its whitespace gone, nothing of it reaches the output, and the file is left readable by its owner alone. 164 checks to 171.
2026-09-19 | rooms | edited | stories/adl.html, stories/ghaffar.html, stories/hadi.html, stories/index.html, stories/jabbar.html, stories/razzaq.html, stories/sabur.html, stories/shakur.html, stories/wadud.html | THE NINE STORY ROOMS FINALLY GET THE DIET THE OTHER FORTY TWO GOT. Batch six put fifty one old rooms on it: the twenty one kilobyte baked header replaced by a small hidden stub, the night painted in the page's own head before any stylesheet lands, and the four scripts noor-fx.js loads on demand taken off. Forty two pages got it, the nine under stories/ did not, and the suite has carried eight failures ever since, reading 42/51 in every one of them. There was no reason: scripts/oldrooms.py names all fifty one in PAGES and run against the nine it refuses none, they were simply never passed to it. 545,598 bytes to 312,941, a header of 21,199 bytes down to 491 on each, and noor-search.js, noor-ramadan.css, noor-hijri.js and noor-ramadan.js off all nine. The suite reads 349 passed and 0 failed, from 341 and 8, and the check that matters most is the last of the eight: every night override the shell carries for a room's own rules now lands on the page with no script needed, 1,368 of 1,368, which is the one standing between a reader with no JavaScript and a page of dark ink on a dark ground. Not taken on trust. The same script was run again in a separate tree and produced the nine files byte for byte, so the transformation is deterministic rather than lucky, and stories/adl.html was then opened in a real browser twice, once with scripting and once without. With script: night ground, ink at #FFFEF7, the baked header hidden because noor-fx.js hangs its own bar off it, seventeen links reachable. Without: the same ground and the same readable ink, the header turned back on, and the noscript menu of five doors rendered, thirteen links reachable, no way to be stranded.
2026-09-19 | films | edited | tools/films/shortplate.py, tests/films-workflow.mjs | A RITE IS DATED BY ITS OWN CALENDAR. Eleven films about the Hajj had been sitting compiled in films/ since the day they were written, and ten of them could not be compiled: running shortplate.py over their briefs refused all ten with the same line, the first two lines give no date. Every one of them gives its date in the first two lines. Tawaf is on the tenth of Dhul Hijjah. Ihram runs until the tenth. Arafah is the ninth. The gate could not read any of it, because the law it enforces was written for films about things that happened once, and the only when it knew how to recognise was a year, a decade, a century or about a thousand years ago. For a rite that comes round every year, a Gregorian year is not a vaguer answer than the ritual date, it is the wrong answer, and nobody asking when tawaf happens wants to hear 1206. So a Hijri month satisfies the law, from a closed list of the twelve. Nothing else was loosened, and one thing was tightened: the century words matched on their own with no century behind them, so hajjmap's "the road from the eighth to the thirteenth", which is a range of DAYS in Dhul Hijjah, was read as the thirteenth century and waved through. That is the only brief in the library that passed on a bare ordinal and it passed by a misreading; every other brief that cites a century writes the word, and the ordinal now has to carry it. The eleven were never wrong, only never gated: recompiling all thirty five plate briefs against the new gate produces thirty five files byte for byte identical to the thirty five committed, with none failing, so the change admits exactly the eleven and moves nothing else. The test runs the shipped check_words itself rather than restating its regex, over eleven cases: four Gregorian forms accepted, four Hijri forms accepted, and three refusals that matter, a bare ordinal that is really a day, a brief with no when at all, and a brief whose when arrives on line three, which is one line too late. Reverting either half of the change fails five of them. 33 checks to 45.
2026-09-19 | tests | edited | tests/mushaf-engine.mjs | THE RECITATION'S FALLBACK CHAIN WAS RIGHT AND THE TEST'S NUMBER WAS WRONG. The suite carried one failing check: after every mirror is refused it expected at least eight requests for the dead verse and counted six, its own comment saying why it expected eight, "four mirrors, refused, then two retry rounds of four", and four is not how many mirrors there are. quran.html builds the chain as the chosen reciter at his own bitrate, the same reciter at the other bitrate in case the service carries both, then Alafasy, who is always there, then everyayah; the Alafasy entry is skipped when Alafasy IS the chosen reciter, because it would be the same URL twice, and he is the default and so the one this test runs with. Three mirrors, each asked twice, is six, which was the whole chain tried and retried with nothing skipped, and the check calling it a failure was the only thing wrong. quran.html is untouched and nothing about the player changed. The fix is not to lower eight to six: a count like that says nothing about what the reader is promised and drifts the moment a reciter is added, which is how it came to be wrong in the first place. The promise is that a verse is never silently dropped because one host is down, so that is what is checked now, from the URLs actually requested: every mirror in the chain was tried rather than just the first, the chain reaches more than one provider, and every one of them was asked again before the player gave up, with the failure text naming the hosts it found so a future failure says what broke. Proved by taking everyayah back out of quran.html's chain and watching two of the three fail, naming cdn.islamic.network as the only provider left, then putting it back. 12 passed and 1 failed, to 15 passed and 0 failed, in a real browser.
2026-09-19 | house | edited | .gitignore | Importing shortplate.py to exercise its own gate leaves a python cache directory beside it. It has no business in the repository.
2026-09-19 | films | edited | tools/films/publish-shorts.sh, tools/films/shortmanifest.py | Two things the shelf run printed, one that could cost real work and one cosmetic. THE ONE THAT MATTERS: the closing words still said to open out/main in Finder, drag everything in it onto the repository's upload page, and that "the house never pushes". That law was lifted on 17 September and the Director has committed every shelf since. Worse than merely stale, out/main is not a folder of shelf files: it holds forty seven pages of the site and the api scripts, copied there so the manifest can read captions off them, and dragging the lot onto main by hand is a way to lay a stale copy of a page over a fresh one and never notice. The words now name the one file this run makes that belongs on main, and say plainly not to carry the rest. THE COSMETIC ONE: shortmanifest.py used datetime.utcfromtimestamp, which python deprecates and intends to remove, so every run printed a warning into the owner's terminal; it hands back a naive datetime that claims to be UTC and carries nothing saying so, which is why it is going, and the aware form asks for UTC out loud, timezone.utc and not datetime.UTC, which exists only on 3.11 and later while this has to run wherever the owner's python happens to be. The dates had to not move, so the whole manifest was rebuilt with the patched file against the owner's own briefs, with DeprecationWarning promoted to an error so a surviving warning would have failed the run outright, and the output came back byte for byte identical to the old one, 105,585 bytes across fifty four rows.
2026-09-19 | films | edited | reels/index.json | FORTY ONE FILMS ON THE SHELF, EVERY ONE OF THEM WITH ITS COVER. The shelf carried twenty two. It carries forty one, and nineteen of those have never been seen by anybody. All forty one were rendered again from the top after the mechanism engine landed, so the twenty two already live are not the same films they were: their pictures move now, and every one of them draws its score from the first bar of the nasheed rather than from a window chosen by loudness. One hundred and twenty three files went up, forty one tall, forty one wide and forty one covers, with nothing in the error log, and every row on the shelf carries a real https address for all three. The merge's own law held with nothing to refuse: a film whose cover had not arrived would have been left off, and a run accounting for fewer films than the shelf already had would have written nothing at all. The covers are the part that nearly went wrong, since the twenty two sitting in out/deliver were cut on 17 September from masters that were replaced the next day, and without the freshness rule made a few hours earlier this run would have published twenty two films behind stills of the versions where nothing moved, silently; watching it run, every cover it cut belonged to a film that already had one, and every one was cut again. Verified three ways rather than taken from the script's own report: the ledger records 123 of 123 uploads, the store itself lists all three files for a film that did not exist before this run with their content types and sizes written minutes before, and the suite reads 171 checks green against the merged shelf with reels, reels-kinds, social, telegram, youtube and the dash guard green beside it.
2026-09-19 | films | edited | tools/films/plates.sh, tools/films/noor.py | A FILM DROPS ITS OWN OLD FRAMES, so the disk stops filling with builds that no longer exist. The frames cache is keyed on a hash of the film, the page, spec.py and every hand written file in web/, and that key is right: change a line of a film or a line of the motion engine and the frames already drawn are no longer the film, so a fresh cache begins and resuming stays honest rather than stitching half of one cut to half of another. Nothing ever removed the cache left behind. This morning the owner asked whether he could delete his finished films to make room for eleven more, with the disk at 98 percent. He did not need to. frames/ held 178 directories for 82 films, and 96 of them belonged to builds that no longer existed and could never be reused again: 48.7 GB. Every engine change this week had quietly cost a generation of every film in the library, and the films themselves, 15 GB, were never the problem. So a film now drops its own older generations once its current ones have actually been drawn, and three things keep that narrow. It runs after the encode and the mux, so the master exists before anything is removed. It asks noor.py where the current frames are, through a new read only --cells that prints the directory and stops, rather than working the hash out a second time in shell, which is how a copy of a rule drifts from the rule. And it does nothing whatever unless that directory is really on disk, so a failed render or an unanswered question can never be read as "none of these are current, remove them all". --cells answers before anything is created, so asking where the frames would go never brings that directory into being. Proved on a fixture holding a film's current cache, two older generations of it, the same film's other shape and a different film entirely: the two older generations go, the other three are untouched, and nothing else is removed; then the case that matters, with the current cache deleted first, where it removes nothing at all. And proved against the owner's own machine before his frames were touched: --cells was asked for six film and shape pairs in a separate tree and named exactly the six directories that had survived the clear out, which is a second implementation agreeing with the first about which caches are current.
2026-09-19 | social | edited | api/_schedule.js, api/social.js, tests/reels-kinds.mjs | THE PICKER IS TOLD WHAT HAS ALREADY GONE OUT, instead of finding out too late. The 08:00 reel was refused by all five networks at once, each saying it already had that reel, and the slot went out to nobody. The owner saw five green labels and reasonably read them as five duplicate posts; they were the opposite, the duplicate guard refusing five sends, and nothing was posted twice or posted at all. The guard was right every time and the morning was still lost, because the guard is the last line: sendOne asks it once a send is already under way, per channel, and all it can do is refuse. WHY A SLOT LANDS ON SOMETHING ALREADY SENT. The walk that picks a reel is stateless by design, computed from the date and the shelf rather than remembered, and it visits every card of a kind before it comes round, but its stride and offset are laid out against the length of the list, so the shelf's own words are true, a new shelf is a new walk. Measured against the real shelf, putting the films on it moved 60 of the 84 slots in a fortnight onto a different card, across name, know, verse and word, and going from 22 films to 41 moved 8 more. A moved slot can land on something posted days earlier, and then the guard refuses it and the slot is spent on silence. So the walk is handed what has actually been sent and steps past it, and three things make that safe. It steps rather than filters: filtering the list would change its length, and the stride and offset are chosen against the length, so filtering would re-shuffle the very walk it was meant to steady, while stepping leaves every other card exactly where it was and skips only the occupied cells. It changes nothing when it is told nothing, the first pick being returned before the set is consulted, so a store that is down, a caller that predates this and every existing test all get the old answer, proved rather than asserted by running the old module and the new one side by side over six months of dates, six halves and a named day, 2,184 picks, every one identical. And it never answers nothing: a kind entirely spent inside the window returns its plain pick, because nothing is the fault being fixed, not the remedy. The read is recentlyPosted(date) in social.js, one HGETALL of the per channel hash, the same read findDuplicate already does first, reduced to the reel ids some channel has had inside the window. The per channel hash and not the posted ledger, because the ledger names a reel only once EVERY live network has it, so a reel that reached three networks of five would be missing from it while those three would certainly refuse it again. runDue reads it once for the whole run, so two slots of one run cannot pick the same reel either, and a pinned repair names its own reel, never reaches the picker and costs no read at all. pick() got the same treatment as pickStep(), for the same reason, since it is laid out against its list's length too and is what the day's card and the stand in kinds walk by: without it, 26 slots of 552 still came back unmoved. 35 checks to 44.
2026-09-19 | films | edited | reels/index.json | FIFTY TWO FILMS ON THE SHELF: THE HAJJ IS ON IT NOW. The eleven that were written, refuted twice, given forty seven corrections, and then sat compiled for two days unable to pass the house's own date gate are live: tawaf, sa'i, ihram, the five miqat, Arafah, the jamarat, labbayk, the three ranks, the three ways, the six days and the map of the whole thing. Every one of the fifty two rows carries a real address for its tall file, its wide file and its cover, 156 files on the shelf, and nothing in the error log. All eleven covers were cut from the masters rendered the night before rather than kept from anything earlier, which is the rule that went in the previous morning and is now working on films that never existed when it was written. The run itself is the shape the pipeline was meant to have: the forty one films already up were skipped whole, their delivery copies being newer than their masters and the shelf already holding them at the same size, so only the eleven were encoded and sent.
2026-09-21 | site | edited | index.html, tests/home2.mjs | THE FRONT DOOR SAYS WHAT IT IS ABOUT. A stranger arrives from a reel, and the page met them with a warm, open question about nothing in particular. "What do you want to know?" names no subject, and the line under it says "the whole religion", which names no religion. The word Islam did not appear above the fold at all, except as the third item in the list of suggested questions, by which point most people have decided. Two words, and the gold moved onto them: the question keeps its warmth, which is the whole personality of that screen, and the emphasis finally points at something, where it had been falling on "to know", which carries no information. The lede needed no edit, because once the headline names Islam, "the whole religion" has something to refer back to, and that reads better than saying Islam twice in four lines. The third string is the one nobody sees on the page and almost everybody sees somewhere else. The title named the house and no subject at all, and it is the browser tab, the headline of every search result, the headline of every shared link and the name of every bookmark; it now reads "NOOR, Learn Islam, from the sources", with the brand still leading, the subject arriving immediately, and the last four words not a slogan but the promise the lede already makes, that every answer carries where it came from. og:title matches, so a link shared into a chat carries it too, and the description tag already named the Qur'an, Hadith and Islamic history and needed nothing. The headline was pinned by a test, which failed exactly as it should have; its intent held through the change, so it keeps its intent and gains the reason. The title was pinned by nothing at all, which is why it could sit like that, and it is pinned now, and so is og:title. 187 checks to 189.
2026-09-21 | social | edited | api/social.js, api/_schedule.js, admin2.html, tests/reels-kinds.mjs | A FILM COMES ROUND EVERY NINETY ONE DAYS AND THE GUARD WATCHED TWENTY ONE. The owner found one of his films posted a second time. He was right, and the hole was a number. THE ARITHMETIC NOBODY HAD DONE. A card comes round again when its kind runs out, so the honest gap between one card and itself is its pool divided by how often the rota asks for that kind. Measured against the real shelf: word 369 days, know 315, light 277, verse 231, name 173, dua 126, and short 91. The shortest honest gap in the whole library belongs to the films, and the guard looked back twenty one days: seventy days in which a repeat was possible and nothing was watching. WHY IT FIRED THIS WEEK RATHER THAN EVER. The walk re-lays itself whenever the shelf grows, which is written into it, and the shelf went from 22 films to 41 to 52 inside one week because the house put them there. A film sent on day 10 could be offered again on day 25, straight past a guard that only looked back to day 21. Twenty one was never a considered number for the guard: it was the number that kept backfillPosted's walk cheap, and it leaked into a function that walks nothing, since the guard and the picker each read one hash and then apply a cutoff, so their reach costs nothing whatever. The two are separate constants now, and the guard looks back sixty days, three times what it was, wide enough to cover any re-laying of the walk and thirty one days clear of the shortest gap the rota legitimately produces, so it never refuses a card whose turn has honestly come. AND THE NUMBER IS NO LONGER LEFT TO MEMORY. The test works the shortest cycle out from the real shelf and the real rota and holds the window under it, with a fortnight of margin: put it back to 21 and it fails, raise it to 100 and it fails from the other side, naming the kind it would start refusing, so adding films, or asking for them more often, will say so before the owner does. THE PICKER NO LONGER LEANS ON THE WINDOW AT ALL. It already stepped past anything sent; it now knows WHEN each was sent, so once a kind has been round once it takes the one sent longest ago rather than the first it steps on, the widest gap available, always, whatever the window says. recentlyPosted returns a Map instead of a Set for that, and a Map answers has() and size exactly as a Set does, so a caller that only asks whether needs no change and a caller that passes a plain Set still works. AND THE CONSOLE STOPS MISLEADING ITS ONLY READER. It said "(already had it)", which is a state and not an action, and the owner read it the one other way English allows, that the network already had the reel and here it was again; five of those on one slot read as five duplicate posts, the exact opposite of what happened. It says "not sent (already up)" now, because a label read by one person under pressure has to say what was done. 44 checks to 51.
2026-09-21 | social | edited | api/social.js, tests/reels-kinds.mjs | THE GUARD CAN ONLY REFUSE WHAT IT REMEMBERS, and it remembered nothing before 16 September. The owner found the same short on his YouTube twice, sent on 7 September and again on the 21st, fourteen days apart. That is inside the guard's window even as it stood before that morning, so the widening made earlier the same day does not explain it, and he was told so plainly. WHAT THIS ONE IS. The guard reads the per channel hash first, and that hash was created on 16 September and only ever grows forward, so it has no record of any send before that date. When it misses, the guard falls back to the older ledger, and that ledger names a reel only once EVERY live network has it, which is a rule about when the shelf may retire a file and a poor one about whether anything was posted. So the 7 September send was written down in neither place; on the 21st the guard was asked about it, found nothing anywhere, and could only say yes. It was not wrong, it was blind. backfillPosted exists precisely to write the past down into the hash the guard reads, and it was built with a reach of twenty one days, because twenty one is what keeps its own walk cheap. That is a statement about cost, not about how far back the history goes, and this library has been posting since August. Its reach is the caller's now, clamped between one day and four hundred, and it reports the span it actually covered so nobody has to guess whether it reached far enough; the console action passes a days parameter through, and the bound itself stays where it was by default, so nothing that calls it today behaves differently. Five checks on the walk: the old default when asked for nothing, the reach when asked for one, the span it names, a silly number clamped rather than run, and nought falling back rather than walking nothing. 51 checks to 56.
2026-09-21 | social | note | the live store | backfillPosted was then run against the real store with its reach opened to two hundred days, and wrote 319 sends the guard had never known about, the span it reported covering 6 March to 21 September. Every one of those is now a send the picker steps past and the guard can refuse, including the 7 September short that came round again a fortnight later. The duplicate copy itself is the owner's to remove, since the house does not delete from his accounts: the 21 September upload is the one to go and the 7 September one is the one to keep.
2026-09-22 | social | added | api/_reconcile.js, api/social.js, admin2.html, tests/reconcile.mjs, .github/workflows/tests.yml | WHAT THE NETWORKS ACTUALLY HOLD, AGAINST WHAT THE HOUSE BELIEVES. The owner found a film on his YouTube twice, 7 and 21 September, by scrolling his own Studio: the house had never once asked a network what it carries, so a gap between the ledger and the world could only be found by a person looking. Masterplan section 11 says it in one line: never treat a successful API response as proof of publication, verify the platform result. A new read, from the console's System room ("What the networks actually hold") or GET /api/social?action=reconcile&net=youtube, walks the channel's own uploads playlist and every video's status, walks every slot record back to the channel's oldest video, and names five things: a send we recorded that the network does not have, a reel the network has that no record of ours kept, a reel posted more than once (only the doubled copies are marked, the oldest to keep, the lawful Short and wide pair of a film never offered for removal), a send recorded as ok that YouTube then rejected or failed, and a title two cards could both claim. It writes nothing anywhere, and the house never deletes from the owner's accounts. Clean is a claim and is only made when everything was checked: a walk that did not finish, a status read answered in part, a send that kept no id, an ambiguous title, or a reel older than the records all keep it from being clean, and a network that cannot be enumerated at all (Telegram's bot API cannot list history; the Meta and Pinterest adapters are not written yet) answers "not checked, and so not cleared", never clean. When fewer than half the recorded sends are found the absences are listed under a caveat rather than asserted, because that shape is far likelier a measurement fault than a lost channel. A video is named back to its reel by the platform id where a record kept one, and otherwise by its title, built through buildSlot and the channel shaper exactly as the poster builds it. The first cut built titles from the card's own name, and a film's Short goes up under its HOOK: the refuter fed it the owner's own case and it answered "the two sides agree". Its tests had passed beside it because they computed their titles the same wrong way, so every title a network holds in tests/reconcile.mjs is now written as a literal, and putting the first index back fails eight checks including the 7 September case. Two review rounds by the refuter, thirteen findings, every one fixed with a check: a film retired from the shelf read as a duplicate, a film's wide video offered for removal, a video handed back on two pages counted twice, a rejected upload read as clean, a hung line to Google with no bound, and more. The end to end check drives the shipped sentTo and reconcile through a stand in store and channel and proves every call on the wire was a read. Quota: 21 units for 500 videos, 81 at the cap. 79 checks, the suite added to CI, every other suite unchanged and green.
2026-09-22 | social | added | api/social.js, admin2.html, tests/reconcile.mjs | WHAT THE NETWORK HOLDS BECOMES WHAT THE GUARD REMEMBERS. The reconciliation's first run against the real channel found eight reels on YouTube more than once (the Zakat film, Rukhsah, al-Biruni's India, al-Basit, al-Jalil, Laylat al-Qadr, Al-Baqarah 2:37 three times, and Al-Isra 17:36, which has left the shelf and so could only be seen by title), every copy confirmed against YouTube's own public record, every duplicate dated 8 to 21 September and none since the guard's repairs of the 21st. It also found that several of those sends were never recorded anywhere, so the duplicate guard, which remembers only what a record names, had no memory that YouTube held them and the picker could have sent them there again. teachGuard runs the pass and writes, for every reel it can name on the network, the latest day that reel appears there into the per channel hash the guard and the picker both read, only where the guard knew nothing or an older day, never winding a later day back. It writes to the house's own store only and never to a network, it is idempotent, and it is run by the owner from the System room ("Teach the guard"). 86 checks, the end to end run proving the Zakat film is remembered at its latest day, a fully recorded film is left untouched, a second run writes nothing, a later day is never overwritten, and the picker steps past what was taught.
2026-09-22 | content | edited | prophets-data.js, .build-src/nodes.src.js, scripts/patches/seerah.mjs, scripts/patches/characters-v2a.mjs, scripts/patches/places.mjs, node/37.json, node/38.json, node/39.json, node/40.json, node/41.json, node/42.json, node/46.json, nodes.js, nodes-index.js, characters.js, places.js, build/heroes.json, heroes.html, tests/citations.mjs | EVERY NARRATION NAMES WHERE IT CAN BE FOUND, AND NAMES IT ONE WAY (audit content-001 and content-002, both high). Each citation was checked against the collection itself on sunnah.com, text against text, and the numbers of the ones corrected were confirmed a second time by hand; nothing was applied on a guess, and no text in the library was found misquoting its source. The three sentences cited under different numbers in different rooms now carry one citation everywhere: the Prophet's final words are Bukhari 4586 and 4438 (4463 has the plain words without the list of the favoured), "spread salam, feed food" is Ibn Majah 3251, the one narration that carries all four clauses, and the banner at Khaybar is Bukhari 3702 and Muslim 2406, the pair that carries both loves. All 26 of the prophets' narrations, which named only "Sahih al-Bukhari" or "Sahih Muslim", now carry their numbers, two of them aligned with the chapters that quote the same text (Idris at the Night Journey, Bukhari 3887 and Muslim 162; the prayer of Dawud, Bukhari 1131 and Muslim 1159), and "I was sent only to perfect noble character" is al-Adab al-Mufrad 273. Uwais al-Qarani's mention in Sahih Muslim is numbered, 2542. The four Qur'an verses filed as narrations in the chapters on Badr, Hudaybiyyah and the Opening leave hadith[]; each was already in the chapter's own verses. The two narrations of Uhud and the Trench that carried a blank source were exact duplicates of sourced ones beside them and are merged; "Hamza is the master of the martyrs" is al-Tabarani, al-Mu'jam al-Kabir 2958, graded sahih by al-Suyuti, in both rooms that quote it; "Salman is one of us, the Ahl al-Bayt" is named for what it is, a sirah report (Ibn Hisham, and Ibn Sa'd's Tabaqat). Nine reports that are sirah or companions' sayings keep their honest sirah label; they are not narrations with a number to find. tests/citations.mjs reads all 265 narrations in the prophets, the chapters, the companions and the places and fails on a blank source, a verse filed as a narration, an unnumbered prophet narration, or one sentence cited under disjoint numbers anywhere; on its first run it found the Idris and Dawud pairs above, which the audit had not.
2026-09-22 | lantern | added | api/_jev.js, api/illuminations.js, tests/jev.mjs | THE LANTERN HAS A JUDGE BESIDE ITS WRITER. The free models that write the Verse Lamp, the Seeker's Question, the Hidden Thread and the Friday Light were checked only for being prose. Jev (TypeSafe AI, released 15 September), a model that cannot write and answers only yes or no with a probability, now reads each piece before it is saved and asks five things of the text itself: does it quote the Prophet or a companion (refused outright in a verse reflection or a Friday Light, where nothing handed to the writer contained a saying), does it give a hadith number the house has not checked, does it issue a ruling, does it speak with contempt of anyone, and does it keep to what it was asked about. Any one at or past one half sends the tile to its hand written light. Jev cannot say whether a reflection is true to its verse, since the Lantern does not hold every verse's English, and nothing here claims it does; it sees the dangerous shape of a piece of writing, which is how a free model does harm in an Islamic library. It is reached through Vercel's AI Gateway, free, authenticated by the deployment's own OIDC token, so there is no key to create or leak and no package. It can only take a reflection away: if it cannot be reached, answers late or answers in part, the Lantern behaves exactly as before and the answer's gate field says "unavailable". The owner can see it work at /api/illuminations?kind=gate, which runs a reflection it must pass and one it must refuse through the same judge and shows every score. tests/jev.mjs drives the real Verse Lamp end to end with a free model inventing a hadith with a number, a ruling and a quotation, and proves no reader sees it.
2026-09-22 | social | added | api/_reconcile.js, api/social.js, admin2.html, tests/reconcile-meta.mjs | THE DUPLICATE CHECK ON INSTAGRAM, FACEBOOK AND THREADS. The reconciliation YouTube got today now asks the other three what they hold, from the System room ("Check Instagram", "Check Facebook", "Check Threads", and "Teach the guard" for each). They carry no title, so a post is named by the first twelve words of its caption, links, tags, punctuation and vowel marks set aside, against captions built by the poster's own shaper; on the real shelf twelve words name 1,508 of 1,509 reels, and the one pair that opens alike is reported as ambiguous rather than guessed. Only reels on Instagram and videos on Threads are read, because until 9 September the day's cards went to the grid and to Threads as ordinary posts, a word card opens with the same words as that word's reel, and 421 old card posts would otherwise have been called the older copy of a reel, the real reel marked for removal, and the guard taught that reels never sent there had been (found by the refuter before it shipped). Instagram and Threads sometimes record the id of the upload container rather than the post, so on those two a record and an unrecorded post of the same reel within two days are one send; never on Facebook, whose ids are exact, and never across a longer gap, where the pair is a removed send and an unrecorded resend and both are reported. The daily cards' stories vanish in a day and are in no list, so the ledger side reads only the reel slots here. A clean verdict on these networks says plainly that it cannot see an upload rejected after an ok, since they give no upload status.
2026-09-22 | site | edited | api/page.js, vercel.json, tests/rooms.mjs | A VERSE ROOM NEVER WAITS ON A STRANGER FOR ITS ARABIC (audit seo-008). A verse room fetched its Arabic and English from api.alquran.cloud while the reader waited, and when that service was slow the page printed "the text of this verse is on the Mushaf" and was cached at the edge for a day as a success. The Uthmani text of all 6,236 verses has always been in the reels' own table beside the function; it is read from there now. Only the English still comes from outside, and a page missing it says so to the cache, which asks again in ten minutes rather than a day; in a range, once the English fails for one verse it is not asked for the rest, where waiting on each in turn had made a long range take minutes. The room gains a headline naming the verse, the only room that had none, and on the 1,280 verses the library has already written about, a "What it says" section with the verse's sense and its words one by one, in the house's own checked words, nothing generated; the page description falls back to that sense. verse/*.json is added to the function's files, without which the section would have rendered in every test and on no live page (found by the refuter).
2026-09-22 | search | edited | llms.txt, index.html | llms.txt, the page AI assistants read to understand the site, still described the library of August: it now lists every room that has its own address (a verse, a surah, a Light, a chapter, a prophet, a companion, a place, a Name, a dictionary term, the day) with the pattern of each address and both sitemaps, states that narrations are cited by collection and number and checked, and says only what is true of the evidence badges and the dictionary. The home page's structured data names its subject, Islam, as the Wikidata entity Q432 and its Wikipedia article.
2026-09-22 | house | edited | CURRENT_STATE.md | The state document, six days stale, rewritten to the house as it stands tonight, including what the owner has postponed until funding so no session starts it.
2026-09-23 | lantern | edited | api/_jev.js, api/illuminations.js, tests/jev.mjs | The first live look at the judge, with the console unlocked, showed it alive and wrong in one place: it refused the bad sample for all four right reasons and also refused the sound reflection, because the relevance question asked whether it was "a reflection on the meaning of Qur'an 3:190" and Jev is never shown the verse (0.27). Left alone, every Verse Lamp would have gone to the hand written light. Relevance is now asked of what the text shows, whether it is about God, faith, the Qur'an, prayer or a believer's life, with a matching form for the Friday light and the Thread. The owner's probe now sends one sound piece of each of the four kinds and one bad one, and calls the gate working only when all four pass and the bad one does not, since a judge that refuses everything is as broken as one that refuses nothing. It also takes an owner only calibration (text and questions in, raw scores out, nothing saved) so a question can be measured before it is changed.
2026-09-23 | social | note | the live store | The duplicate check was run live on Instagram (106 reels, 82 in the record, 21 never recorded, 11 sent more than once), Facebook (115, 80, 32, 19) and Threads (84, 78, 4, 6); nothing was absent and nothing was rejected. The copies date from 5 to 21 September, the late ones because the first send was never recorded. As with YouTube the owner leaves the copies; the guard was taught all three networks (21, 23 and the Threads set written), so none of those reels can go to them again inside the window.
2026-09-23 | mushaf | edited | quran.html, tests/mushaf-reading.mjs, tests/word-align-report.mjs | The reading view the owner approved on 17 September ("The Mushaf, Rebuilt") is now the Mushaf itself, built only from what the library has already written. Tap one word on a verse that has notes and a sheet gives its Arabic, how it sounds and what it means, from the same verse/<s>.json the verse rooms read; a word is matched to its gloss only when the verse's words and the notes' words are the same in number, so a gloss can never land on the wrong word (checked against the live Uthmani text: 1,264 of the 1,279 written verses match word for word, the rest simply play as before). A transliteration line, off by default, under every written verse. One "How you read" sheet for the size of the Arabic, translation, transliteration and Day or Night; Day is a real parchment page, every text in the room checked at 4.5:1 or better by a contrast audit that runs in the test. The top bar and the player step aside while you read down and return the moment you scroll up; the player never leaves while a recitation is playing or paused, and keyboard focus brings them back. Each verse ends in its number sealed in a small round mark, as a printed mushaf sets it, never counted by the word by word light and never picked up when the text is copied. The notes button now reads "Study this verse". Two older faults fixed on the way: At-Tin and Al-Qadr (95 and 97) showed the Bismillah twice, because their text writes a shadda on the ba that the strip did not allow, so the light also ran four words behind the voice; and a "Read alongside" link carried a dash. The notes file is still fetched only when a reader asks for something in it. Reviewed twice by the refuter; tests: mushaf 45, mushaf-engine 15, quran-player 205, mushaf-reading 96, all passing.
2026-09-23 | mushaf | edited | quran.html, tests/mushaf-reading.mjs | Seen on the live site within the hour: on a wide screen the word sheet and "How you read" were centred with no width of their own and shrank round their content, 140px around one word on a desktop. Both now take a reading width (34rem, less on a narrow window), and the test opens both at 1280px and measures them. The CI list gains the citations, the judge and the Meta reconciliation suites (edited in GitHub's own editor, since the house's key cannot change workflow files).
